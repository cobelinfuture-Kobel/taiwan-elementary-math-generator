import test from "node:test";
import assert from "node:assert/strict";
import {readFileSync} from "node:fs";
import {auditG5AU06P05F63Projection,G5A_U06_P05F63_INCLUDED_RELATIONS as RELATIONS,G5A_U06_P05F63_KP_ID as KP,G5A_U06_P05F63_PROTECTED_SIBLING_KP_IDS as PRIOR,G5A_U06_P05F63_REQUIRED_CAPABILITY_IDS as CAPS,G5A_U06_P05F63_SOURCE_ID as SRC,G5A_U06_P05F63_SPEC_IDS as SPECS} from "../../site/modules/curriculum/registry/g5a-u06-missing-addend-structure-selector-projection-p05f63.js";
import * as preQ063Selector from "../../site/modules/curriculum/registry/batch-a-selector-p05f62-extension.js";
import {auditP05F63PublicSelectorComposition,getVisibleBatchAKnowledgePoint,getVisiblePatternGroupsForKnowledgePoint,listBatchAKnowledgePointAvailabilityBySource,listVisibleBatchAKnowledgePoints,resolveVisiblePatternSpecIdsForKnowledgePoint} from "../../site/modules/curriculum/registry/batch-a-selector-p05f63-extension.js";
import {auditPublicUiCapabilityBinding,resolvePublicUiCapabilityBinding} from "../../site/modules/curriculum/public/public-ui-capability-binding-p05f63.js";
import {buildG5AU06P05F63Question,generateG5AU06P05F63Questions,validateG5AU06P05F63Answer,validateG5AU06P05F63Question} from "../../site/modules/curriculum/batch-a/g5a-u06-missing-addend-structure-runtime-p05f63.js";
import {buildBatchABrowserPlan,generateBatchABrowserQuestions} from "../../site/modules/curriculum/batch-a/batch-a-browser-generator-p05f63.js";
import {buildBatchABrowserWorksheetDocument} from "../../site/modules/curriculum/batch-a/batch-a-browser-worksheet-p05f63-extension.js";
import {renderG5AU06Q063Diagram} from "../../site/modules/renderer/g5a-u06-missing-addend-diagram-p05f63.js";
import {renderRectangleSquareAreaFormulaDiagram} from "../../site/modules/renderer/rectangle-square-area-formula-diagram.js";
const read=p=>JSON.parse(readFileSync(new URL(`../../${p}`,import.meta.url),"utf8"));
const implementation=read("data/curriculum/full-product/p05f/q063-g5a-u06-missing-addend-structure-implementation.json"),impact=read("data/project/change-impact/P05F_W5_Q063.impact.json"),validation=read("data/project/validation-plans/P05F_W5_Q063.validation.json");
const request=(extra={})=>({sourceId:SRC,selectionMode:"singleKnowledgePoint",selectedKnowledgePointIds:[KP],questionMode:"diagram",questionCount:20,generationSeed:"q063-test",...extra});
test("Q063 materializes fraction missing-addend semantics while triangle remains representation only",()=>{assert.equal(implementation.status,"IMPLEMENTATION_MATERIALIZED_AWAITING_FOCUSED_CI");assert.equal(implementation.queueAuthority.queuePosition,63);assert.deepEqual(implementation.queueAuthority.knowledgePointIds,[KP]);assert.deepEqual(implementation.queueAuthority.requiredW5CapabilityIds,CAPS);assert.deepEqual(implementation.productContract.includedRelations,RELATIONS);assert.equal(implementation.productContract.triangleConfigurationRole,"REPRESENTATION_ONLY");assert.equal(implementation.productContract.genericTriangleGeometryValidation,false);assert.deepEqual(auditG5AU06P05F63Projection(),{ok:true,errors:[],counts:{knowledgePoints:1,patternGroups:1,patternSpecs:2,formalMappings:1}});});
test("Q063 promotes only the final G5A-U06 missing-addend KP",()=>{const before=preQ063Selector.listVisibleBatchAKnowledgePoints().filter(x=>x.sourceId===SRC).map(x=>x.knowledgePointId);assert.deepEqual([...before].sort(),[...PRIOR].sort());assert.equal(preQ063Selector.getVisibleBatchAKnowledgePoint(KP),null);const a=auditP05F63PublicSelectorComposition();assert.equal(a.ok,true,a.errors.join("\n"));const s=listBatchAKnowledgePointAvailabilityBySource(SRC),after=listVisibleBatchAKnowledgePoints().filter(x=>x.sourceId===SRC).map(x=>x.knowledgePointId);assert.deepEqual([...after].sort(),[...PRIOR,KP].sort());assert.ok(getVisibleBatchAKnowledgePoint(KP));assert.equal(getVisiblePatternGroupsForKnowledgePoint(KP).length,1);assert.deepEqual(resolveVisiblePatternSpecIdsForKnowledgePoint(KP,"diagram"),SPECS);assert.equal(s.hiddenPendingKnowledgePointIds.includes(KP),false);assert.equal(s.notSelectableKnowledgePointIds.includes(KP),false);});
test("Q063 public binding is exact-rational diagram-only and keeps geometry semantics closed",()=>{const a=auditPublicUiCapabilityBinding();assert.equal(a.ok,true,a.errors.join("\n"));const b=resolvePublicUiCapabilityBinding(request());assert.equal(b.blocked,false);assert.equal(b.questionType,"diagram");assert.equal(b.applicationSuitability,"APPLICATION_NOT_APPLICABLE");assert.equal(b.exactFractionEqualityRequired,true);assert.equal(b.unknownAddendReconstructionRequired,true);assert.equal(b.triangleConfigurationRepresentationOnly,true);assert.equal(b.genericGeometryPropertyUsed,false);assert.equal(b.geometryConstructionUsed,false);assert.deepEqual(b.requiredCapabilityIds,CAPS);});
for(const patternSpecId of SPECS)test(`Q063 ${patternSpecId} has 240 deterministic unique exact-rational variants`,()=>{const a=generateG5AU06P05F63Questions({questionCount:240,patternSpecIds:[patternSpecId],generationSeed:"stable"}),b=generateG5AU06P05F63Questions({questionCount:240,patternSpecIds:[patternSpecId],generationSeed:"stable"});assert.equal(a.ok,true,a.errors.join("\n"));assert.deepEqual(a.questions,b.questions);assert.equal(new Set(a.questions.map(q=>q.questionSignature)).size,240);for(const q of a.questions){assert.equal(validateG5AU06P05F63Question(q).ok,true);assert.equal(q.metadata.genericGeometryPropertyUsed,false);assert.equal(q.metadata.triangleSideOrAnglePropertyUsed,false);assert.equal(validateG5AU06P05F63Answer(q,{numerator:q.answerFraction.numerator*2,denominator:q.answerFraction.denominator*2}).ok,true);}});
test("Q063 validator fails closed unless the submitted fraction reconstructs the same total",()=>{const q=buildG5AU06P05F63Question({variant:17,patternSpecId:SPECS[1]});assert.equal(validateG5AU06P05F63Question(q).ok,true);assert.equal(validateG5AU06P05F63Answer(q,{numerator:q.answerFraction.numerator+1,denominator:q.answerFraction.denominator}).ok,false);assert.equal(validateG5AU06P05F63Question({...q,metadata:{...q.metadata,genericGeometryPropertyUsed:true}}).ok,false);assert.equal(validateG5AU06P05F63Question({...q,geometryDiagram:{...q.geometryDiagram,triangleSideOrAngleProperty:true}}).ok,false);});
test("Q063 browser plan worksheet and shared renderer are usable at D1",()=>{const p=buildBatchABrowserPlan(request({questionCount:8}));assert.equal(p.questionCountMax,240);assert.deepEqual(p.selectedKnowledgePointIds,[KP]);const g=generateBatchABrowserQuestions(request({questionCount:8}));assert.equal(g.ok,true,g.errors.join("\n"));assert.equal(g.questions.length,8);const w=buildBatchABrowserWorksheetDocument(request({questionCount:8,includeAnswerKey:true,printLayout:{columns:2,rowsPerPage:4,showAnswerKeyPage:true}}));assert.equal(w.ok,true,w.errors.join("\n"));assert.equal(w.worksheetDocument.questionCount,8);assert.equal(w.worksheetDocument.answerKeyItems.length,8);assert.equal(w.worksheetDocument.metadata.structuredFractionDisplay,true);const direct=renderG5AU06Q063Diagram(g.questions[1].geometryDiagram),shared=renderRectangleSquareAreaFormulaDiagram(g.questions[1].geometryDiagram);assert.equal(shared,direct);assert.match(direct,/data-representation="fraction-missing-addend-diagram"/);assert.match(direct,/□/);assert.match(direct,/給定總和/);});
test("Q063 validation contract stays SHARED_RUNTIME_BOUNDED",()=>{assert.equal(impact.expectedDerivedGate,"SHARED_RUNTIME_BOUNDED");assert.equal(validation.policyId,"UNIT_INCREMENTAL_VALIDATION_V1");assert.deepEqual(validation.lanes.SHARED_RUNTIME_BOUNDED.map(x=>x.gateId),["GLOBAL_CONTRACTS","TARGETED_ROUTE_REPLAY"]);assert.equal(JSON.stringify(validation).includes("FULL_REPOSITORY"),false);});
