import test from "node:test";
import assert from "node:assert/strict";
import {auditP03F21PublicSelectorComposition,listBatchAKnowledgePointAvailabilityBySource,resolveVisiblePatternSpecIdsForKnowledgePoint} from "../../site/modules/curriculum/registry/batch-a-selector-p03f21-extension.js";
import {buildBatchABrowserWorksheetDocument} from "../../site/modules/curriculum/batch-a/batch-a-browser-worksheet-p03f21-extension.js";
import {getCurrentPixelSourceSummary} from "../../site/pixel/pixel-registry-bridge.js";
import {getFullProductPublicControlProfile} from "../../site/modules/curriculum/registry/full-product-public-control-profiles.js";
const options={sourceId:"g5a_u01_5a01",selectedKnowledgePointIds:["kp_g5a_u01_decimal_read_place"],selectedPatternGroupIds:["pg_g5a_u01_decimal_read_place_numeric"],questionMode:"numeric",questionCount:20,generationSeed:"p03f21-current",includeAnswerKey:true,printLayout:{columns:2,rowsPerPage:6,showAnswerKeyPage:true}};
test("P03F21 Classic selector adds G5A-U01 with one visible and seven hidden KPs",()=>{assert.equal(auditP03F21PublicSelectorComposition().ok,true);assert.equal(listBatchAKnowledgePointAvailabilityBySource("g5a_u01_5a01").visibleCount,1);assert.equal(resolveVisiblePatternSpecIdsForKnowledgePoint(options.selectedKnowledgePointIds[0]).length,1);assert.deepEqual(getFullProductPublicControlProfile("g5a_u01_5a01").questionTypeControl.options.map(r=>r.value),["numeric"]);});
test("P03F21 shared worksheet emits 20 questions and 20 answer keys",()=>{const result=buildBatchABrowserWorksheetDocument(options);assert.equal(result.ok,true,JSON.stringify(result.errors));assert.equal(result.worksheetDocument.questionCount,20);assert.equal(result.worksheetDocument.answerKeyItems.length,20);assert.deepEqual(result.generation.allocation.map(r=>r.questionCount),[20]);assert.equal(result.worksheetDocument.metadata.applicationExpansion,false);});
test("P03F21 historical selector stays intact while Pixel current G5A-U01 advances through Slice048 to eight visible KPs",()=>{const summary=getCurrentPixelSourceSummary("g5a_u01_5a01");assert.equal(summary.visibleKnowledgePoints.length,8);assert.deepEqual(new Set(summary.visibleKnowledgePoints.map(row=>row.knowledgePointId)),new Set(["kp_g5a_u01_decimal_read_place","kp_g5a_u01_decimal_compose_decompose","kp_g5a_u01_decimal_add_sub","kp_g5a_u01_decimal_compare","kp_g5a_u01_place_value_factor_relation","kp_g5a_u01_decimal_round_estimate","kp_g5a_u01_missing_digit_inequality","kp_g5a_u01_inverse_rounding_range"]));});
