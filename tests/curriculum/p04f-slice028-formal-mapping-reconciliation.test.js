import test from "node:test";
import assert from "node:assert/strict";
import fs from "node:fs";
const sliceAuthority=JSON.parse(fs.readFileSync(new URL("../../data/curriculum/full-product/p04f/slice028-g5a-u04-fraction-measurement-segments-formal-mapping-authority.json",import.meta.url),"utf8"));
const p02eOverrides=JSON.parse(fs.readFileSync(new URL("../../data/curriculum/full-product/p02e/quantity-semantic-role-overrides.json",import.meta.url),"utf8"));
const KP_ID="kp_g5a_u04_fraction_measurement_segments";
test("P04F28 q028 preserves one slice-local source-declared FormalMapping",()=>{assert.equal(sliceAuthority.queue.sliceId,"p04e_q028_r6_g5a_u04_5a04_profile_quantity_measurement_c1");assert.deepEqual(sliceAuthority.source.primaryWitnessPages,[2]);assert.equal(sliceAuthority.knowledgePoints.length,1);assert.equal(sliceAuthority.knowledgePoints[0].knowledgePointId,KP_ID);assert.equal(sliceAuthority.knowledgePoints[0].kpSplitAllowed,false);assert.equal(sliceAuthority.formalMapping.authorityMode,"P04F_SLICE_LOCAL_EXACT");assert.equal(sliceAuthority.formalMapping.relationFamilyId,"SOURCE_DECLARED_QUANTITY_RELATION");assert.deepEqual(sliceAuthority.formalMapping.allowedTargetRoleIds,["PER_GROUP_QUANTITY","GROUP_COUNT"]);assert.equal(p02eOverrides.bindings.some((row)=>row.knowledgePointId===KP_ID),false);assert.equal(sliceAuthority.authorityBoundary.p02eHistoricalDependentInventoryMutable,false);assert.equal(sliceAuthority.authorityBoundary.p02eExactOverrideRequired,false);assert.equal(sliceAuthority.authorityBoundary.p02eConsumerResolutionRequired,false);});
test("P04F28 q028 materializes exactly the two locked PatternSpecs",()=>{const contracts=sliceAuthority.formalMapping.patternTargetContracts;assert.deepEqual(contracts.map((row)=>row.targetRoleId),["PER_GROUP_QUANTITY","GROUP_COUNT"]);assert.deepEqual(sliceAuthority.patternSpecs.map((row)=>row.targetRoleId),["PER_GROUP_QUANTITY","GROUP_COUNT"]);assert.equal(sliceAuthority.authorityBoundary.patternSpecMaterialized,true);assert.equal(sliceAuthority.authorityBoundary.generatorMaterialized,true);assert.equal(sliceAuthority.authorityBoundary.selectorPromoted,true);assert.equal(sliceAuthority.authorityBoundary.worksheetEnabled,true);assert.equal(sliceAuthority.authorityBoundary.q029Touched,false);});
