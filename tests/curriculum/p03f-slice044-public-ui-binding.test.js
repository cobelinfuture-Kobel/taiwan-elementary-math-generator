import test from "node:test";
import assert from "node:assert/strict";
import { PUBLIC_UI_SURFACES,auditPublicUiCapabilityBinding,resolvePublicUiCapabilityBinding } from "../../site/modules/curriculum/public/public-ui-capability-binding-p03f44.js";
import { G5A_U01_P03F44_SOURCE_ID,P03F44_KP_IDS } from "../../site/modules/curriculum/registry/g5a-u01-rank10-decimal-selector-projection-p03f44.js";
test("P03F44 public binding exposes both q044 KPs on Classic and Pixel",()=>{const audit=auditPublicUiCapabilityBinding();assert.equal(audit.ok,true,audit.errors.join("\n"));for(const surfaceId of [PUBLIC_UI_SURFACES.CLASSIC,PUBLIC_UI_SURFACES.PIXEL]){for(const kp of P03F44_KP_IDS){const binding=resolvePublicUiCapabilityBinding({sourceId:G5A_U01_P03F44_SOURCE_ID,surfaceId,selectionMode:"singleKnowledgePoint",selectedKnowledgePointIds:[kp]});assert.equal(binding.blocked,false);assert.deepEqual(binding.selectedKnowledgePointIds,[kp]);assert.equal(binding.questionType,"numeric");assert.equal(binding.compatiblePatternGroupIds.length,1);}}});
test("P03F44 mixed binding stays numeric-only with two q044 groups",()=>{const binding=resolvePublicUiCapabilityBinding({sourceId:G5A_U01_P03F44_SOURCE_ID,surfaceId:PUBLIC_UI_SURFACES.CLASSIC,selectionMode:"mixedKnowledgePointsSameUnit",selectedKnowledgePointIds:P03F44_KP_IDS});assert.equal(binding.blocked,false);assert.equal(binding.selectedKnowledgePointCount,2);assert.equal(binding.compatiblePatternGroupIds.length,2);assert.deepEqual(binding.depthOptions,[]);assert.deepEqual(binding.contextOptions,[]);});
