import test from "node:test";
import assert from "node:assert/strict";
import { PUBLIC_UI_SURFACES,auditPublicUiCapabilityBinding,resolvePublicUiCapabilityBinding } from "../../site/modules/curriculum/public/public-ui-capability-binding-p03f45.js";
import { G5B_U04_P03F45_KP_ID,G5B_U04_P03F45_SOURCE_ID } from "../../site/modules/curriculum/registry/g5b-u04-rank10-decimal-times-decimal-selector-projection-p03f45.js";
import { listVisibleBatchAKnowledgePoints } from "../../site/modules/curriculum/registry/batch-a-selector-p03f45-extension.js";
test("P03F45 public binding exposes q045 on Classic Pixel and 404",()=>{const audit=auditPublicUiCapabilityBinding();assert.equal(audit.ok,true,audit.errors.join("\n"));for(const surfaceId of Object.values(PUBLIC_UI_SURFACES)){const binding=resolvePublicUiCapabilityBinding({sourceId:G5B_U04_P03F45_SOURCE_ID,surfaceId,selectionMode:"singleKnowledgePoint",selectedKnowledgePointIds:[G5B_U04_P03F45_KP_ID]});assert.equal(binding.blocked,false);assert.deepEqual(binding.selectedKnowledgePointIds,[G5B_U04_P03F45_KP_ID]);assert.equal(binding.questionType,"numeric");assert.equal(binding.compatiblePatternGroupIds.length,1);assert.deepEqual(binding.depthOptions,[]);assert.deepEqual(binding.contextOptions,[]);}});
test("P03F45 mixed G5B-U04 binding exposes all three numeric groups only",()=>{const sourceKps=listVisibleBatchAKnowledgePoints().filter((row)=>row.sourceId===G5B_U04_P03F45_SOURCE_ID).map((row)=>row.knowledgePointId),binding=resolvePublicUiCapabilityBinding({sourceId:G5B_U04_P03F45_SOURCE_ID,surfaceId:PUBLIC_UI_SURFACES.CLASSIC,selectionMode:"mixedKnowledgePointsSameUnit",selectedKnowledgePointIds:sourceKps});assert.equal(sourceKps.length,3);assert.equal(binding.blocked,false);assert.equal(binding.selectedKnowledgePointCount,3);assert.equal(binding.compatiblePatternGroupIds.length,3);assert.equal(binding.compatiblePatternGroups.every((group)=>group.effectiveQuestionType==="numeric"),true);assert.deepEqual(binding.depthOptions,[]);assert.deepEqual(binding.contextOptions,[]);});
