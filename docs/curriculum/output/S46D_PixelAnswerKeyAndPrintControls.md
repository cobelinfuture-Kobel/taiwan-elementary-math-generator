S46D_PixelAnswerKeyAndPrintControls

CURRENT_MAJOR_TASK = S46_PixelUIGenerationIntegration
CURRENT_SUBTASK = S46D_PixelAnswerKeyAndPrintControls
TASK_STATUS = IMPLEMENTED_CI_AND_DEPLOY_PENDING
OUTPUT = Public Pixel answer-key output summary and print controls

## 1. Scope Check

```text
- Preserve Classic UI at /
- Keep Pixel UI at /pixel/
- Reuse the shared worksheetDocument and iframe preview
- Reuse shared printPreviewFrame()
- Keep answer-key inclusion controlled by the existing shared worksheet state
- Do not fork generator / validator / renderer / registry / PatternSpec
- Do not continue unrelated curriculum-content tasks
```

## 2. Files Created

```text
site/pixel/pixel-print-controller.js
site/pixel/pixel-print-surface.js
tests/ui/pixel-print-controller.test.js
tests/ui/pixel-print-surface.test.js
```

## 3. Files Modified

```text
site/pixel/index.html
site/pixel/pixel-selector.css
tests/ui/pixel-live-preview-surface.test.js
tests/ui/pixel-generate-button-surface.test.js
```

## 4. Implementation Notes

The public print path is now:

```text
Pixel include-answer-key setting
-> shared worksheet plan
-> shared generator / validator
-> shared worksheetDocument
-> shared HTML preview iframe
-> summarizePixelPrintAvailability()
-> printPixelWorksheet()
-> shared printPreviewFrame()
-> iframe contentWindow.print()
```

The public page now exposes:

```text
pixel-output-summary
pixel-print-button
pixel-print-surface.js
```

Behavior:

```text
- Before generation, print is disabled.
- Successful output with answer pages shows "列印題目卷＋答案頁".
- Successful output without answer pages shows "列印題目卷".
- Output summary reports question pages and answer-page counts.
- Changing grade / semester / unit / mode / KP / question count / ordering / seed / print layout / answer-key setting marks the print surface stale and disables printing until regeneration.
- Print delegates to the shared iframe print path.
```

## 5. Shared-Core Integrity

No Pixel-only worksheet renderer or print renderer was created.

`pixel-print-controller.js` imports the existing:

```text
site/assets/browser/pipeline/render-preview-frame.js
```

and calls `printPreviewFrame()`.

Answer-key pages continue to come from the authoritative shared `worksheetDocument.answerKeyItems` / `answerKeyPages` generated by the existing pipeline.

## 6. Test Coverage

New and updated tests verify:

```text
- answer-key enabled output is identified as 題目卷＋答案頁;
- answer-key disabled output is identified as 僅題目卷;
- shared preview-frame focus/print path is called;
- unsuccessful generation cannot print;
- public HTML exposes output summary and disabled initial print button;
- print surface subscribes to authoritative generation results;
- stale changes disable print until regeneration;
- S46B/S46C surface assertions advance to Preview / Print stage wording.
```

## 7. Acceptance Status

```text
Answer-key output summary = STATIC PASS
Print button surface = STATIC PASS
Shared printPreviewFrame reused = STATIC PASS
Stale-setting print guard = STATIC PASS
No renderer / generator / validator fork = STATIC PASS
Math CI Readback = PENDING
Deploy GitHub Pages = PENDING
```

## 8. Closeout

1. 本任務縮短了哪一段距離？
   - It moved Pixel UI from live preview only to a printable question/answer output path.
2. 推進了哪一個系統節點？
   - WebUI / Renderer / AnswerKey / Print execution integration.
3. 是否解除 blocker？
   - Yes. The blocker "Need Pixel answer-key / print execution controls" is removed at implementation level.
4. 是否增加新的 blocker？
   - No new functional blocker. CI and Pages readback remain required.
5. 下一個最短有效步驟是什麼？
   - S46D_CIReadbackAndPagesDeployReadback.

GOAL_DISTANCE_BEFORE = D1_WEBUI_PIXEL_LIVE_PREVIEW_DEPLOYED
GOAL_DISTANCE_AFTER = D1_WEBUI_PIXEL_ANSWER_PRINT_WRITTEN_CI_DEPLOY_PENDING
DISTANCE_REDUCED = Pixel UI can now identify answer-page output, block stale printing, and invoke the shared iframe print path.
REMAINING_BLOCKERS = ["Need Math CI Readback for S46D", "Need Deploy GitHub Pages readback for S46D", "Need S47 HTML output / print QA", "Need full Pixel functional QA"]
NEXT_SHORTEST_STEP = S46D_CIReadbackAndPagesDeployReadback
STOP_REASON = ci_and_deploy_readback_required
BLOCKER_TYPE = CI_AND_DEPLOY_READBACK_REQUIRED
LAST_COMPLETED_STATUS = S46D_IMPLEMENTED
REQUIRED_OPERATOR_ACTION = Provide Math CI Readback and Deploy GitHub Pages success for the latest S46D commit.
NEXT_RESUME_TASK = S46D_CIReadbackAndPagesDeployReadback
