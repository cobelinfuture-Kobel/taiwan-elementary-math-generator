# P03F W3 Direct Product Vertical Slice 007 Scope Freeze

```text
PROGRAM_ID = FULL_PRODUCT_LINE_D0_V1
TASK_ID    = P03F_W3DirectProductVerticalSlice007Implementation
STATUS     = SCOPE_FROZEN_PENDING_AUTHORITY_DIAGNOSTIC
```

Only queue position 7 may be implemented in this milestone. Queue position 8 and later slices are forbidden.

The required lineage is:

```text
source evidence
→ KnowledgePoint
→ Tag Registry
→ FormalMapping
→ PatternSpec
→ required W3 capability consumers
→ shared generator / validator
→ Classic / Pixel selector
→ WorksheetDocument / answer key
→ committed HTML / PDF
→ Chromium / visual / hash acceptance
→ E6 D0 closeout
```

Numeric and application outputs must remain separate when the curriculum authority classifies the KnowledgePoint as application-compatible. No parallel product pipeline, second context engine, or slice008 work is allowed.
