name: P03F12 One-Shot Core Bootstrap

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

concurrency:
  group: p03f12-core-bootstrap-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: false

jobs:
  bootstrap:
    if: github.head_ref == 'p03f-w3-direct-product-vertical-slice012-v1'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - run: npm install
      - name: Materialize Slice012 core
        shell: bash
        run: |
          set -euxo pipefail
          python - <<'PY'
          from pathlib import Path

          files = {
          'site/modules/curriculum/registry/g4b-u08-equivalence-cross-product-selector-projection.js': r'''export const G4B_U08_EQUIVALENCE_CROSS_PRODUCT_KP_ID = "kp_g4b_u08_equivalence_cross_product";
          export const G4B_U08_EQUIVALENCE_CROSS_PRODUCT_GROUP_ID = "pg_g4b_u08_equivalence_cross_product_numeric";
          export const G4B_U08_EQUIVALENCE_CROSS_PRODUCT_SPEC_ID = "ps_g4b_u08_equivalence_cross_product_equivalent_numeric";
          import { G4B_U08_SOURCE_ID, G4B_U08_UNIT_CODE, G4B_U08_UNIT_TITLE } from "./g4b-u08-equivalent-fraction-selector-projection.js";
          const clone = (value) => value == null ? value : JSON.parse(JSON.stringify(value));
          const freeze = (value) => { if (!value || typeof value !== "object" || Object.isFrozen(value)) return value; for (const nested of Object.values(value)) freeze(nested); return Object.freeze(value); };
          export const G4B_U08_EQUIVALENCE_CROSS_PRODUCT_PATTERN_GROUPS = freeze([{ patternGroupId: G4B_U08_EQUIVALENCE_CROSS_PRODUCT_GROUP_ID, sourceId: G4B_U08_SOURCE_ID, unitCode: G4B_U08_UNIT_CODE, unitTitle: G4B_U08_UNIT_TITLE, displayName: "交叉乘積判定等值", primaryKnowledgePointId: G4B_U08_EQUIVALENCE_CROSS_PRODUCT_KP_ID, knowledgePointIds: [G4B_U08_EQUIVALENCE_CROSS_PRODUCT_KP_ID], supportClass: "A", mode: "numeric", publicQuestionMode: "numeric", representationTag: "fraction_equivalence_cross_product", representationTags: ["fraction", "equivalence", "cross_product", "boolean_judgment"], patternSpecIds: [G4B_U08_EQUIVALENCE_CROSS_PRODUCT_SPEC_ID], allocationPolicy: "single_canonical_pattern_spec", visibilityStatus: "visible", holdReason: null }]);
          export const G4B_U08_EQUIVALENCE_CROSS_PRODUCT_ROWS = freeze([{ knowledgePointId: G4B_U08_EQUIVALENCE_CROSS_PRODUCT_KP_ID, sourceId: G4B_U08_SOURCE_ID, unitCode: G4B_U08_UNIT_CODE, unitTitle: G4B_U08_UNIT_TITLE, displayName: "交叉乘積判定等值", canonicalNameZh: "交叉乘積判定等值", mode: "numeric", questionMode: "numeric", supportClass: "A", visibilityStatus: "visible", selectorStatus: "visible", holdReason: null, applicationClassification: "APPLICATION_NOT_APPLICABLE", canonicalPatternGroupIds: [G4B_U08_EQUIVALENCE_CROSS_PRODUCT_GROUP_ID], canonicalPatternSpecIds: [G4B_U08_EQUIVALENCE_CROSS_PRODUCT_SPEC_ID], patternGroupIds: [G4B_U08_EQUIVALENCE_CROSS_PRODUCT_GROUP_ID], patternSpecIds: [G4B_U08_EQUIVALENCE_CROSS_PRODUCT_SPEC_ID], qaStatusLabel: "P03F_SLICE012_CANDIDATE", productionUse: "full_product_w3_slice012_candidate" }]);
          export const G4B_U08_EQUIVALENCE_CROSS_PRODUCT_SELECTOR_PROJECTION = freeze({ taskId: "P03F_W3DirectProductVerticalSlice012Implementation", sourceId: G4B_U08_SOURCE_ID, status: "SECOND_W3_KP_NUMERIC_PUBLIC_D0_CANDIDATE", knowledgePointCount: 1, patternGroupCount: 1, patternSpecCount: 1, excludedKnowledgePointIds: ["kp_g4b_u08_fraction_compare_cross_product", "kp_g4b_u08_unlike_denominator_add_sub", "kp_g4b_u08_fraction_decimal_conversion", "kp_g4b_u08_fraction_number_line_distance", "kp_g4b_u08_mixed_fraction_order_constraints"], publicSelectionEnabled: true, sharedPipelineRequired: true, applicationModeAllowed: false });
          export function listG4BU08EquivalenceCrossProductSelectorRows() { return clone(G4B_U08_EQUIVALENCE_CROSS_PRODUCT_ROWS); }
          export function getG4BU08EquivalenceCrossProductSelectorRow(id) { return id === G4B_U08_EQUIVALENCE_CROSS_PRODUCT_KP_ID ? clone(G4B_U08_EQUIVALENCE_CROSS_PRODUCT_ROWS[0]) : null; }
          export function listG4BU08EquivalenceCrossProductPatternGroups(id) { return id === G4B_U08_EQUIVALENCE_CROSS_PRODUCT_KP_ID ? clone(G4B_U08_EQUIVALENCE_CROSS_PRODUCT_PATTERN_GROUPS) : []; }
          export function resolveG4BU08EquivalenceCrossProductPatternSpecIds(id) { return id === G4B_U08_EQUIVALENCE_CROSS_PRODUCT_KP_ID ? [G4B_U08_EQUIVALENCE_CROSS_PRODUCT_SPEC_ID] : []; }
          export function auditG4BU08EquivalenceCrossProductSelectorProjection() { const errors = []; if (G4B_U08_EQUIVALENCE_CROSS_PRODUCT_ROWS.length !== 1) errors.push("P03F12_KP_COUNT_INVALID"); if (G4B_U08_EQUIVALENCE_CROSS_PRODUCT_PATTERN_GROUPS.length !== 1) errors.push("P03F12_GROUP_COUNT_INVALID"); if (G4B_U08_EQUIVALENCE_CROSS_PRODUCT_PATTERN_GROUPS[0].patternSpecIds.length !== 1) errors.push("P03F12_SPEC_COUNT_INVALID"); if (G4B_U08_EQUIVALENCE_CROSS_PRODUCT_SELECTOR_PROJECTION.applicationModeAllowed) errors.push("P03F12_APPLICATION_SCOPE_VIOLATION"); return Object.freeze({ ok: errors.length === 0, errors: Object.freeze(errors), counts: Object.freeze({ knowledgePoints: 1, patternGroups: 1, patternSpecs: 1 }) }); }
          ''',
          'site/modules/curriculum/registry/batch-a-selector-p03f12-extension.js': r'''export * from "./batch-a-selector-p03f11-extension.js";
          import * as base from "./batch-a-selector-p03f11-extension.js";
          import { G4B_U08_SOURCE_ID, G4B_U08_EQUIVALENT_FRACTION_KP_ID } from "./g4b-u08-equivalent-fraction-selector-projection.js";
          import { G4B_U08_EQUIVALENCE_CROSS_PRODUCT_KP_ID, G4B_U08_EQUIVALENCE_CROSS_PRODUCT_SELECTOR_PROJECTION, auditG4BU08EquivalenceCrossProductSelectorProjection, getG4BU08EquivalenceCrossProductSelectorRow, listG4BU08EquivalenceCrossProductPatternGroups, listG4BU08EquivalenceCrossProductSelectorRows, resolveG4BU08EquivalenceCrossProductPatternSpecIds } from "./g4b-u08-equivalence-cross-product-selector-projection.js";
          const clone = (value) => value == null ? value : JSON.parse(JSON.stringify(value));
          const rows = Object.freeze(listG4BU08EquivalenceCrossProductSelectorRows().map((row) => Object.freeze(row)));
          const baseAvailability = base.listBatchAKnowledgePointAvailabilityBySource(G4B_U08_SOURCE_ID);
          export { G4B_U08_EQUIVALENCE_CROSS_PRODUCT_SELECTOR_PROJECTION };
          export const BATCH_A_KNOWLEDGE_POINT_REGISTRY_METADATA = base.BATCH_A_KNOWLEDGE_POINT_REGISTRY_METADATA;
          export const BATCH_A_SELECTOR_AVAILABILITY = Object.freeze({ ...base.BATCH_A_SELECTOR_AVAILABILITY, visibleCount: base.BATCH_A_SELECTOR_AVAILABILITY.visibleCount + 1, publicSourceCount: 25, bySourceId: Object.freeze({ ...base.BATCH_A_SELECTOR_AVAILABILITY.bySourceId, [G4B_U08_SOURCE_ID]: Object.freeze({ ...(baseAvailability ?? {}), sourceId: G4B_U08_SOURCE_ID, visibleCount: 2, hiddenPendingCount: 5, notSelectableCount: 0, publicSelectorStatus: "slice012_two_w3_knowledge_points_candidate_visible", canonicalReachableKnowledgePointCount: 2, canonicalReachableKnowledgePointIds: Object.freeze([G4B_U08_EQUIVALENT_FRACTION_KP_ID, G4B_U08_EQUIVALENCE_CROSS_PRODUCT_KP_ID]), compatibilityProjection: "full_product_w3_shared_pipeline", publicDropdownCutoverTask: "P03F_W3DirectProductVerticalSlice012Implementation" }) }) });
          export function listVisibleBatchAKnowledgePoints() { return [...base.listVisibleBatchAKnowledgePoints(), ...rows.map(clone)]; }
          export function listBatchAKnowledgePointAvailabilityBySource(sourceId) { return sourceId === G4B_U08_SOURCE_ID ? clone(BATCH_A_SELECTOR_AVAILABILITY.bySourceId[sourceId]) : base.listBatchAKnowledgePointAvailabilityBySource(sourceId); }
          export function getVisibleBatchAKnowledgePoint(id) { return getG4BU08EquivalenceCrossProductSelectorRow(id) ?? base.getVisibleBatchAKnowledgePoint(id); }
          export function getVisiblePatternGroupsForKnowledgePoint(id) { const groups = listG4BU08EquivalenceCrossProductPatternGroups(id); return groups.length ? groups : base.getVisiblePatternGroupsForKnowledgePoint(id); }
          export function resolveVisiblePatternSpecIdsForKnowledgePoint(id, mode = null) { const ids = resolveG4BU08EquivalenceCrossProductPatternSpecIds(id); return ids.length ? ids : base.resolveVisiblePatternSpecIdsForKnowledgePoint(id, mode); }
          export function auditP03F12PublicSelectorComposition() { const errors = [...auditG4BU08EquivalenceCrossProductSelectorProjection().errors]; const allRows = listVisibleBatchAKnowledgePoints(); const ids = allRows.map((row) => row.knowledgePointId); if (new Set(ids).size !== ids.length) errors.push("P03F12_SELECTOR_DUPLICATE_KP"); const availability = listBatchAKnowledgePointAvailabilityBySource(G4B_U08_SOURCE_ID); if (!availability || availability.visibleCount !== 2 || availability.hiddenPendingCount !== 5) errors.push("P03F12_SELECTOR_AVAILABILITY_INVALID"); const sourceRows = allRows.filter((row) => row.sourceId === G4B_U08_SOURCE_ID); const groups = sourceRows.flatMap((row) => getVisiblePatternGroupsForKnowledgePoint(row.knowledgePointId)); const specs = [...new Set(groups.flatMap((row) => row.patternSpecIds ?? []))]; if (sourceRows.length !== 2 || groups.length !== 2 || specs.length !== 4) errors.push("P03F12_CURRENT_SOURCE_SURFACE_INVALID"); return Object.freeze({ ok: errors.length === 0, errors: Object.freeze(errors), counts: Object.freeze({ addedKnowledgePoints: 1, currentSourceKnowledgePoints: 2, currentSourcePatternGroups: 2, currentSourcePatternSpecs: 4, publicSources: 25 }) }); }
          ''',
          'site/modules/curriculum/batch-a/source-pattern-full-product-p03f12-extension.js': r'''import { getBatchABrowserPatternDefinition as baseGetDefinition, getBatchAPatternSpecIdsForSource as baseGetPatternIds } from "./source-pattern-full-product-p03f11-extension.js";
          import { G4B_U08_SOURCE_ID } from "../registry/g4b-u08-equivalent-fraction-selector-projection.js";
          import { G4B_U08_EQUIVALENCE_CROSS_PRODUCT_KP_ID, G4B_U08_EQUIVALENCE_CROSS_PRODUCT_GROUP_ID, G4B_U08_EQUIVALENCE_CROSS_PRODUCT_SPEC_ID } from "../registry/g4b-u08-equivalence-cross-product-selector-projection.js";
          export const P03F12_REQUIRED_CAPABILITY_IDS = Object.freeze(["cap_fraction_arithmetic", "cap_fraction_domain_validator", "cap_fraction_number_system"]);
          export const G4B_U08_EQUIVALENCE_CROSS_PRODUCT_PATTERN_DEFINITION = Object.freeze({ sourceId: G4B_U08_SOURCE_ID, title: "交叉乘積判定等值｜數字題", kind: "g4bU08EquivalenceCrossProduct", operation: "cross_product_equivalence", operationFamilyId: "cross_product_equivalence", operationModelId: "op_g4b_u08_equivalence_cross_product", knowledgePointId: G4B_U08_EQUIVALENCE_CROSS_PRODUCT_KP_ID, patternGroupId: G4B_U08_EQUIVALENCE_CROSS_PRODUCT_GROUP_ID, patternSpecId: G4B_U08_EQUIVALENCE_CROSS_PRODUCT_SPEC_ID, mode: "NUMERIC", questionMode: "numeric", requestedUnknownRole: "equivalent", givenRoles: Object.freeze(["leftNumerator", "leftDenominator", "rightNumerator", "rightDenominator"]), answerType: "boolean", canonicalExpressions: Object.freeze(["equivalent = leftNumerator * rightDenominator == rightNumerator * leftDenominator"]), canonicalSkillIds: Object.freeze([G4B_U08_EQUIVALENCE_CROSS_PRODUCT_KP_ID]), skillTags: Object.freeze(["fraction", "equivalence", "cross_product", G4B_U08_SOURCE_ID]), difficultyTags: Object.freeze(["exact_integer_cross_products", "boolean_judgment", "full_product_w3_slice012"]), requiredCapabilityIds: P03F12_REQUIRED_CAPABILITY_IDS, applicationClassification: "APPLICATION_NOT_APPLICABLE", globalContextRequired: false, sharedGeneratorAdapterId: "SHARED_OPERATION_FAMILY_GENERATOR_V1", sharedValidatorAdapterId: "SHARED_OPERATION_FAMILY_VALIDATOR_V1", numericDomain: Object.freeze({ denominatorMin: 2, denominatorMax: 20, positiveTerms: true }) });
          export function getBatchABrowserPatternDefinition(id) { return id === G4B_U08_EQUIVALENCE_CROSS_PRODUCT_SPEC_ID ? G4B_U08_EQUIVALENCE_CROSS_PRODUCT_PATTERN_DEFINITION : baseGetDefinition(id); }
          export function getBatchAPatternSpecIdsForSource(sourceId) { return baseGetPatternIds(sourceId); }
          export function validateP03F12PatternDefinitions() { const d = G4B_U08_EQUIVALENCE_CROSS_PRODUCT_PATTERN_DEFINITION; const errors = []; if (d.knowledgePointId !== G4B_U08_EQUIVALENCE_CROSS_PRODUCT_KP_ID) errors.push("P03F12_KP_BINDING_INVALID"); if (d.requestedUnknownRole !== "equivalent") errors.push("P03F12_UNKNOWN_ROLE_INVALID"); if (JSON.stringify(d.givenRoles) !== JSON.stringify(["leftNumerator", "leftDenominator", "rightNumerator", "rightDenominator"])) errors.push("P03F12_GIVEN_ROLE_PARITY_INVALID"); if (JSON.stringify(d.requiredCapabilityIds) !== JSON.stringify(P03F12_REQUIRED_CAPABILITY_IDS)) errors.push("P03F12_CAPABILITY_SET_INVALID"); if (d.globalContextRequired || d.applicationClassification !== "APPLICATION_NOT_APPLICABLE") errors.push("P03F12_APPLICATION_SCOPE_INVALID"); return Object.freeze({ ok: errors.length === 0, errors: Object.freeze(errors), patternSpecCount: 1 }); }
          ''',
          'site/modules/curriculum/batch-a/batch-a-browser-generator-p03f12.js': r'''export * from "./batch-a-browser-generator-p03f11.js";
          import { buildBatchABrowserPlan as buildBasePlan } from "./batch-a-browser-generator-p03f11.js";
          import { getBatchASourceUnit } from "./source-units.js";
          import { G4B_U08_SOURCE_ID } from "../registry/g4b-u08-equivalent-fraction-selector-projection.js";
          import { G4B_U08_EQUIVALENCE_CROSS_PRODUCT_KP_ID, G4B_U08_EQUIVALENCE_CROSS_PRODUCT_GROUP_ID, G4B_U08_EQUIVALENCE_CROSS_PRODUCT_SPEC_ID } from "../registry/g4b-u08-equivalence-cross-product-selector-projection.js";
          const includes = (values, id) => Array.isArray(values) && values.includes(id);
          export function requestsP03F12EquivalenceCrossProduct(options = {}) { return options.sourceId === G4B_U08_SOURCE_ID && (includes(options.selectedKnowledgePointIds, G4B_U08_EQUIVALENCE_CROSS_PRODUCT_KP_ID) || includes(options.selectedPatternGroupIds, G4B_U08_EQUIVALENCE_CROSS_PRODUCT_GROUP_ID) || includes(options.patternSpecIds, G4B_U08_EQUIVALENCE_CROSS_PRODUCT_SPEC_ID)); }
          export function buildBatchABrowserPlan(options = {}) { const plan = buildBasePlan(options); if (!requestsP03F12EquivalenceCrossProduct(options)) return plan; return { ...plan, sourceUnit: { ...getBatchASourceUnit(G4B_U08_SOURCE_ID) }, patternSpecIds: [G4B_U08_EQUIVALENCE_CROSS_PRODUCT_SPEC_ID], allocation: null, questionMode: "numeric", requestedKnowledgePointIds: [G4B_U08_EQUIVALENCE_CROSS_PRODUCT_KP_ID], requestedPatternGroupIds: [G4B_U08_EQUIVALENCE_CROSS_PRODUCT_GROUP_ID], publicControls: { sourceId: G4B_U08_SOURCE_ID, questionMode: "numeric", productWave: "R05-W3", productAdmissionTask: "P03F_W3DirectProductVerticalSlice012Implementation", publicDropdownCutoverTask: "P03F_W3DirectProductVerticalSlice012Implementation", globalContextAuthority: "NOT_APPLICABLE" }, publicPatternSpecInjectionUsed: false, genericFallbackAllowed: false }; }
          ''',
          'site/modules/curriculum/batch-a/equivalence-cross-product-runtime.js': r'''import { buildBatchABrowserPlan } from "./batch-a-browser-generator-p03f12.js";
          import { getBatchABrowserPatternDefinition } from "./source-pattern-full-product-p03f12-extension.js";
          import { G4B_U08_SOURCE_ID } from "../registry/g4b-u08-equivalent-fraction-selector-projection.js";
          import { G4B_U08_EQUIVALENCE_CROSS_PRODUCT_KP_ID, G4B_U08_EQUIVALENCE_CROSS_PRODUCT_GROUP_ID, G4B_U08_EQUIVALENCE_CROSS_PRODUCT_SPEC_ID } from "../registry/g4b-u08-equivalence-cross-product-selector-projection.js";
          const CAPS = Object.freeze(["cap_fraction_arithmetic", "cap_fraction_domain_validator", "cap_fraction_number_system"]);
          const CASES = Object.freeze([
            { leftNumerator:1,leftDenominator:2,rightNumerator:2,rightDenominator:4 },
            { leftNumerator:2,leftDenominator:3,rightNumerator:4,rightDenominator:6 },
            { leftNumerator:3,leftDenominator:5,rightNumerator:9,rightDenominator:15 },
            { leftNumerator:4,leftDenominator:7,rightNumerator:8,rightDenominator:14 },
            { leftNumerator:1,leftDenominator:3,rightNumerator:2,rightDenominator:5 },
            { leftNumerator:3,leftDenominator:4,rightNumerator:6,rightDenominator:10 },
            { leftNumerator:5,leftDenominator:8,rightNumerator:10,rightDenominator:16 },
            { leftNumerator:2,leftDenominator:7,rightNumerator:6,rightDenominator:20 }
          ]);
          function hashSeed(value) { let acc = 2166136261; for (const char of String(value ?? "p03f12")) { acc ^= char.charCodeAt(0); acc = Math.imul(acc, 16777619) >>> 0; } return acc || 1; }
          function buildQuestion(row, index) { const definition = getBatchABrowserPatternDefinition(G4B_U08_EQUIVALENCE_CROSS_PRODUCT_SPEC_ID); const leftCrossProduct = row.leftNumerator * row.rightDenominator; const rightCrossProduct = row.rightNumerator * row.leftDenominator; const equivalent = leftCrossProduct === rightCrossProduct; const promptText = "判斷 " + row.leftNumerator + "/" + row.leftDenominator + " 和 " + row.rightNumerator + "/" + row.rightDenominator + " 是否為等值分數。"; const answerText = equivalent ? "是" : "否"; return Object.freeze({ id: G4B_U08_EQUIVALENCE_CROSS_PRODUCT_SPEC_ID + "-" + index, sourceId: G4B_U08_SOURCE_ID, patternSpecId: G4B_U08_EQUIVALENCE_CROSS_PRODUCT_SPEC_ID, kind: "g4bU08EquivalenceCrossProduct", operation: "cross_product_equivalence", operationFamilyId: "cross_product_equivalence", questionMode: "numeric", promptText, questionText: promptText, blankedDisplayText: promptText, displayText: promptText + " " + answerText, answerText, leftNumerator: row.leftNumerator, leftDenominator: row.leftDenominator, rightNumerator: row.rightNumerator, rightDenominator: row.rightDenominator, leftCrossProduct, rightCrossProduct, equivalent, requestedUnknownRole: "equivalent", finalAnswer: Object.freeze({ value: equivalent, canonicalText: answerText, answerType: "boolean", exact: true }), metadata: Object.freeze({ patternId: definition.patternSpecId, sourceId: definition.sourceId, patternTags: Object.freeze(["full_product_w3_slice012", definition.sourceId, definition.patternSpecId]), skillTags: definition.skillTags, difficultyTags: definition.difficultyTags, curriculumNodeIds: Object.freeze([definition.sourceId]), canonicalSkillIds: definition.canonicalSkillIds, knowledgePointId: definition.knowledgePointId, patternGroupId: definition.patternGroupId, operationFamilyId: definition.operationFamilyId, requestedUnknownRole: definition.requestedUnknownRole, requiredCapabilityIds: definition.requiredCapabilityIds, applicationClassification: "APPLICATION_NOT_APPLICABLE", exactRationalIdentity: true, productAdmissionTask: "P03F_W3DirectProductVerticalSlice012Implementation", generatorAdapterId: "SHARED_OPERATION_FAMILY_GENERATOR_V1", validatorAdapterId: "SHARED_OPERATION_FAMILY_VALIDATOR_V1" }) }); }
          export function canGenerateG4BU08EquivalenceCrossProductQuestions(plan = {}) { return plan.sourceId === G4B_U08_SOURCE_ID && Array.isArray(plan.patternSpecIds) && plan.patternSpecIds.length === 1 && plan.patternSpecIds[0] === G4B_U08_EQUIVALENCE_CROSS_PRODUCT_SPEC_ID; }
          export function validateG4BU08EquivalenceCrossProductQuestion(question = {}) { const errors = []; const add = (code, path) => errors.push({ code, severity: "error", path, message: code }); if (question.sourceId !== G4B_U08_SOURCE_ID || question.metadata?.sourceId !== G4B_U08_SOURCE_ID) add("p03f12_source_mismatch", "sourceId"); if (question.patternSpecId !== G4B_U08_EQUIVALENCE_CROSS_PRODUCT_SPEC_ID || question.metadata?.patternId !== question.patternSpecId) add("p03f12_pattern_mismatch", "patternSpecId"); if (question.metadata?.knowledgePointId !== G4B_U08_EQUIVALENCE_CROSS_PRODUCT_KP_ID || question.metadata?.patternGroupId !== G4B_U08_EQUIVALENCE_CROSS_PRODUCT_GROUP_ID) add("p03f12_kp_group_mismatch", "metadata.knowledgePointId"); const roles = ["leftNumerator","leftDenominator","rightNumerator","rightDenominator"]; if (roles.some((role) => !Number.isInteger(question[role]) || question[role] <= 0) || question.leftDenominator <= 1 || question.rightDenominator <= 1) add("p03f12_fraction_roles_invalid", "leftNumerator"); const left = question.leftNumerator * question.rightDenominator; const right = question.rightNumerator * question.leftDenominator; const equivalent = left === right; if (question.leftCrossProduct !== left || question.rightCrossProduct !== right || question.equivalent !== equivalent) add("p03f12_cross_product_identity_invalid", "leftCrossProduct"); const answerText = equivalent ? "是" : "否"; if (question.answerText !== answerText || question.finalAnswer?.value !== equivalent || question.finalAnswer?.canonicalText !== answerText || question.finalAnswer?.exact !== true) add("p03f12_answer_identity_invalid", "answerText"); if (JSON.stringify(question.metadata?.requiredCapabilityIds) !== JSON.stringify(CAPS)) add("p03f12_capability_set_invalid", "metadata.requiredCapabilityIds"); if (question.questionMode !== "numeric" || question.metadata?.applicationClassification !== "APPLICATION_NOT_APPLICABLE") add("p03f12_application_scope_violation", "questionMode"); if (/(?:算式|_{2,}|答\s*[:：]|\{\{)/.test(String(question.blankedDisplayText ?? ""))) add("p03f12_forbidden_surface_present", "blankedDisplayText"); return { ok: errors.length === 0, errors, warnings: [] }; }
          export function generateG4BU08EquivalenceCrossProductQuestions(options = {}) { const plan = buildBatchABrowserPlan(options); if (!canGenerateG4BU08EquivalenceCrossProductQuestions(plan)) return { ok:false, plan, questions:[], allocation:[], errors:[{ code:"p03f12_plan_not_supported",severity:"error",path:"plan",message:"Slice012 accepts only the admitted cross-product equivalence PatternSpec." }], warnings:[] }; if (plan.questionCount > CASES.length) return { ok:false, plan, questions:[], allocation:[], errors:[{ code:"p03f12_question_count_exceeds_unique_witnesses",severity:"error",path:"questionCount",message:"Slice012 provides at most eight unique witnesses." }], warnings:[] }; const offset = hashSeed(plan.generationSeed) % CASES.length; const selected = Array.from({ length: plan.questionCount }, (_, index) => CASES[(offset + index) % CASES.length]); const questions = selected.map((row,index) => buildQuestion(row,index+1)); const errors = questions.flatMap((question) => validateG4BU08EquivalenceCrossProductQuestion(question).errors); if (new Set(questions.map((row) => row.blankedDisplayText)).size !== questions.length) errors.push({ code:"p03f12_duplicate_prompt_detected",severity:"error",path:"questions",message:"The worksheet contains duplicate prompts." }); return { ok: errors.length === 0 && questions.length === plan.questionCount, plan, questions, allocation:[{ patternSpecId:G4B_U08_EQUIVALENCE_CROSS_PRODUCT_SPEC_ID, questionCount:questions.length }], errors, warnings:[] }; }
          ''',
          'site/modules/curriculum/batch-a/batch-a-browser-validator-p03f12.js': r'''export * from "./batch-a-browser-validator-p03f11.js";
          import { validateBatchABrowserPlan as validateBasePlan, validateBatchABrowserQuestion as validateBaseQuestion, validateBatchABrowserQuestions as validateBaseQuestions } from "./batch-a-browser-validator-p03f11.js";
          import { getBatchABrowserPatternDefinition } from "./source-pattern-full-product-p03f12-extension.js";
          import { validateG4BU08EquivalenceCrossProductQuestion } from "./equivalence-cross-product-runtime.js";
          import { G4B_U08_SOURCE_ID } from "../registry/g4b-u08-equivalent-fraction-selector-projection.js";
          import { G4B_U08_EQUIVALENCE_CROSS_PRODUCT_SPEC_ID } from "../registry/g4b-u08-equivalence-cross-product-selector-projection.js";
          const patternId = (question = {}) => question.patternSpecId ?? question.metadata?.patternId;
          const isP03F12 = (question = {}) => patternId(question) === G4B_U08_EQUIVALENCE_CROSS_PRODUCT_SPEC_ID;
          const issue = (code,path) => ({ code,severity:"error",path,message:code });
          export function validateBatchABrowserPlan(plan = {}) { const ids = Array.isArray(plan.patternSpecIds) ? plan.patternSpecIds : []; if (plan.sourceId !== G4B_U08_SOURCE_ID || ids.length !== 1 || ids[0] !== G4B_U08_EQUIVALENCE_CROSS_PRODUCT_SPEC_ID) return validateBasePlan(plan); const errors=[]; if (!Number.isInteger(plan.questionCount) || plan.questionCount <= 0 || plan.questionCount > 8) errors.push(issue("p03f12_question_count_invalid","questionCount")); if (plan.questionMode !== "numeric") errors.push(issue("p03f12_question_mode_mismatch","questionMode")); if (plan.genericFallbackAllowed !== false) errors.push(issue("p03f12_generic_fallback_must_be_disabled","genericFallbackAllowed")); const d=getBatchABrowserPatternDefinition(ids[0]); if (!d || d.requestedUnknownRole !== "equivalent" || JSON.stringify(d.givenRoles) !== JSON.stringify(["leftNumerator","leftDenominator","rightNumerator","rightDenominator"])) errors.push(issue("p03f12_pattern_definition_invalid","patternSpecIds")); return { ok:errors.length===0,errors,warnings:[] }; }
          export function validateBatchABrowserQuestion(question = {}) { return isP03F12(question) ? validateG4BU08EquivalenceCrossProductQuestion(question) : validateBaseQuestion(question); }
          export function validateBatchABrowserQuestions(questions = []) { if (!questions.some(isP03F12)) return validateBaseQuestions(questions); const errors=[],warnings=[]; for (const [index,question] of questions.entries()) { const result=validateBatchABrowserQuestion(question); errors.push(...result.errors.map((entry)=>({ ...entry,path:"questions["+index+"]."+entry.path }))); warnings.push(...result.warnings); } return { ok:errors.length===0,errors,warnings,infos:[],validatorVersion:"p03f12-g4b-u08-equivalence-cross-product-v1",validatedAt:null }; }
          ''',
          'site/modules/curriculum/batch-a/batch-a-browser-worksheet-p03f12-extension.js': r'''import { paginateAnswerKeyItems, paginateQuestionDisplayModels } from "../../core/index.js";
          import { buildBatchABrowserWorksheetDocument as buildBase } from "./batch-a-browser-worksheet-p03f11-extension.js";
          import { buildBatchABrowserPlan, requestsP03F12EquivalenceCrossProduct } from "./batch-a-browser-generator-p03f12.js";
          import { generateBatchABrowserQuestions } from "./batch-a-browser-question-router.js";
          import { validateBatchABrowserPlan, validateBatchABrowserQuestions } from "./batch-a-browser-validator-p03f12.js";
          import { G4B_U08_SOURCE_ID } from "../registry/g4b-u08-equivalent-fraction-selector-projection.js";
          export const P03F12_WORKSHEET_ADAPTER = Object.freeze({ task:"P03F_W3DirectProductVerticalSlice012Implementation",status:"bounded_shared_numeric_worksheet_adapter_connected",sourceId:G4B_U08_SOURCE_ID,patternSpecCount:1,sharedPagination:true,sharedRenderer:true,parallelPipeline:false });
          const failed=(errors,warnings=[],details={})=>Object.freeze({ ok:false,errors:Object.freeze([...(errors??[])]),warnings:Object.freeze([...(warnings??[])]),worksheetDocument:null,...details });
          export function buildBatchABrowserWorksheetDocument(options={}) { if (!requestsP03F12EquivalenceCrossProduct(options)) return buildBase(options); const plan=buildBatchABrowserPlan(options); const planValidation=validateBatchABrowserPlan(plan); if (!planValidation.ok) return failed(planValidation.errors,planValidation.warnings,{plan,validation:planValidation}); const generation=generateBatchABrowserQuestions(options); if (!generation.ok) return failed(generation.errors,generation.warnings,{plan,generation}); const validation=validateBatchABrowserQuestions(generation.questions); if (!validation.ok) return failed(validation.errors,validation.warnings,{plan,generation,validation}); const requested=options.printLayout??{}; const printLayout=Object.freeze({ paperSize:requested.paperSize??"A4",columns:Math.min(Number.isInteger(requested.columns)?requested.columns:2,2),rowsPerPage:Math.min(Number.isInteger(requested.rowsPerPage)?requested.rowsPerPage:4,4),showQuestionNumbers:requested.showQuestionNumbers!==false,showAnswerKeyPage:options.includeAnswerKey!==false&&requested.showAnswerKeyPage!==false,longTextCardPolicy:"avoidSplit",questionMode:"numeric" }); const models=generation.questions.map((question,index)=>Object.freeze({ questionId:question.id,questionNumber:index+1,patternId:question.patternSpecId,knowledgePointId:question.metadata.knowledgePointId,patternGroupId:question.metadata.patternGroupId,questionNumberText:printLayout.showQuestionNumbers?(index+1)+".":null,promptText:question.blankedDisplayText,displayText:question.displayText,blankedDisplayText:question.blankedDisplayText,answerText:question.answerText,metadataSnapshot:Object.freeze({...question.metadata}),layoutHints:Object.freeze({estimatedTextLength:String(question.blankedDisplayText).length,hasGrouping:false,avoidPageBreakInside:true,representation:"fraction_equivalence_cross_product",longTextCardPolicy:"avoidSplit"})})); const answers=printLayout.showAnswerKeyPage?generation.questions.map((question,index)=>Object.freeze({questionId:question.id,questionNumber:index+1,patternId:question.patternSpecId,knowledgePointId:question.metadata.knowledgePointId,patternGroupId:question.metadata.patternGroupId,promptText:models[index].blankedDisplayText,answerText:question.answerText,metadataSnapshot:models[index].metadataSnapshot,layoutHints:Object.freeze({avoidPageBreakInside:true,representation:"fraction_equivalence_cross_product_answer"})})):[]; const questionPages=paginateQuestionDisplayModels(models,printLayout); const answerKeyPages=printLayout.showAnswerKeyPage?paginateAnswerKeyItems(answers,{...printLayout,columns:2,rowsPerPage:4}):[]; const document=Object.freeze({schemaVersion:"worksheet-document-v1",version:"1",worksheetId:"p03f12-numeric-"+plan.questionCount+"-"+plan.generationSeed,worksheetKind:"batchAWorksheet",title:options.title??"四年級｜交叉乘積判定等值｜數字題",subtitle:"以交叉乘積精確判定兩個分數是否等值",generatedAt:"DETERMINISTIC",configSnapshot:Object.freeze({...plan,printLayout}),orderingMode:plan.ordering,questionCount:generation.questions.length,questionPages:Object.freeze(questionPages),answerKeyPages:Object.freeze(answerKeyPages),sections:Object.freeze([]),generatedQuestions:Object.freeze(generation.questions),questions:Object.freeze(generation.questions),questionDisplayModels:Object.freeze(models),answerKeyItems:Object.freeze(answers),printOptions:Object.freeze({...printLayout,answerKeyColumns:2,answerKeyRowsPerPage:4,showAnswerKey:printLayout.showAnswerKeyPage,answerKeyPlacement:printLayout.showAnswerKeyPage?"afterQuestions":"none"}),publicControls:Object.freeze({sourceId:G4B_U08_SOURCE_ID,questionMode:"numeric",productAdmissionTask:P03F12_WORKSHEET_ADAPTER.task,globalContextRegistry:null}),metadata:Object.freeze({sourceId:G4B_U08_SOURCE_ID,knowledgePointIds:Object.freeze([...(plan.requestedKnowledgePointIds??[])]),applicationClassification:"APPLICATION_NOT_APPLICABLE",worksheetAdapter:P03F12_WORKSHEET_ADAPTER}),batchA:Object.freeze({sourceId:G4B_U08_SOURCE_ID,questionMode:"numeric",selectionMode:plan.selectionMode}),report:Object.freeze({ok:true,errors:Object.freeze([]),warnings:Object.freeze([]),summary:Object.freeze({questionCount:generation.questions.length,questionPageCount:questionPages.length,answerKeyPageCount:answerKeyPages.length})}),summary:Object.freeze({questionCount:generation.questions.length,questionPageCount:questionPages.length,answerKeyPageCount:answerKeyPages.length,numericQuestionCount:generation.questions.length,applicationQuestionCount:0})}); return Object.freeze({ok:true,errors:Object.freeze([]),warnings:Object.freeze([]),worksheetDocument:document,plan,generation,validation,p03f12WorksheetAdapter:P03F12_WORKSHEET_ADAPTER}); }
          ''',
          'tests/curriculum/p03f-slice012-equivalence-cross-product.test.js': r'''import test from "node:test";
          import assert from "node:assert/strict";
          import { materializeP03EW3DirectProductVerticalSliceQueue } from "../../src/curriculum/full-product/p03e-w3-direct-product-vertical-slice-queue.mjs";
          import { buildBatchABrowserPlan } from "../../site/modules/curriculum/batch-a/batch-a-browser-generator-p03f12.js";
          import { generateG4BU08EquivalenceCrossProductQuestions, validateG4BU08EquivalenceCrossProductQuestion } from "../../site/modules/curriculum/batch-a/equivalence-cross-product-runtime.js";
          import { buildWorksheetDocumentFromPlan } from "../../site/assets/browser/pipeline/build-worksheet-document.js";
          import { G4B_U08_SOURCE_ID, G4B_U08_EQUIVALENT_FRACTION_PATTERN_SPEC_IDS } from "../../site/modules/curriculum/registry/g4b-u08-equivalent-fraction-selector-projection.js";
          import { G4B_U08_EQUIVALENCE_CROSS_PRODUCT_KP_ID, G4B_U08_EQUIVALENCE_CROSS_PRODUCT_GROUP_ID, G4B_U08_EQUIVALENCE_CROSS_PRODUCT_SPEC_ID } from "../../site/modules/curriculum/registry/g4b-u08-equivalence-cross-product-selector-projection.js";
          import { listBatchAKnowledgePointAvailabilityBySource, listVisibleBatchAKnowledgePoints } from "../../site/modules/curriculum/registry/batch-a-selector-p03f12-extension.js";
          import { getCurrentPixelRegistrySnapshot, listCurrentPixelSourceOptions, listPixelKnowledgePointsForSource } from "../../site/pixel/pixel-registry-bridge.js";
          const CAPS=["cap_fraction_arithmetic","cap_fraction_domain_validator","cap_fraction_number_system"];
          const plan=(overrides={})=>({sourceId:G4B_U08_SOURCE_ID,selectionMode:"singleKnowledgePoint",selectedKnowledgePointIds:[G4B_U08_EQUIVALENCE_CROSS_PRODUCT_KP_ID],selectedPatternGroupIds:[G4B_U08_EQUIVALENCE_CROSS_PRODUCT_GROUP_ID],questionMode:"numeric",questionCount:8,ordering:"groupedByPattern",includeAnswerKey:true,generationSeed:"p03f12-focused",printLayout:{paperSize:"A4",columns:2,rowsPerPage:4,showQuestionNumbers:true,showAnswerKeyPage:true},...overrides});
          test("P03F12 consumes exact frozen queue position 12",()=>{const slice=materializeP03EW3DirectProductVerticalSliceQueue().queueEntries[11];assert.equal(slice.queuePosition,12);assert.equal(slice.sliceId,"p03e_q012_r6_g4b_u08_4b08_profile_fraction_c1");assert.equal(slice.previousSliceId,"p03e_q011_r6_g4b_u06_4b06_profile_decimal_c1");assert.deepEqual(slice.knowledgePointIds,[G4B_U08_EQUIVALENCE_CROSS_PRODUCT_KP_ID]);assert.deepEqual(slice.requiredW3CapabilityIds,CAPS);});
          test("P03F12 preserves source-unit default while explicit KP selects the new spec",()=>{const explicit=buildBatchABrowserPlan(plan());assert.deepEqual(explicit.patternSpecIds,[G4B_U08_EQUIVALENCE_CROSS_PRODUCT_SPEC_ID]);const sourceDefault=buildBatchABrowserPlan({sourceId:G4B_U08_SOURCE_ID,selectionMode:"sourceUnit",questionMode:"numeric",questionCount:3,generationSeed:"p03f12-source-default"});assert.deepEqual(sourceDefault.patternSpecIds,G4B_U08_EQUIVALENT_FRACTION_PATTERN_SPEC_IDS);});
          test("P03F12 generates eight deterministic unique exact judgments",()=>{const first=generateG4BU08EquivalenceCrossProductQuestions(plan());const second=generateG4BU08EquivalenceCrossProductQuestions(plan());assert.equal(first.ok,true,JSON.stringify(first.errors));assert.deepEqual(first.questions,second.questions);assert.equal(first.questions.length,8);assert.equal(new Set(first.questions.map((row)=>row.blankedDisplayText)).size,8);assert.ok(first.questions.some((row)=>row.equivalent));assert.ok(first.questions.some((row)=>!row.equivalent));for(const q of first.questions){assert.equal(q.equivalent,q.leftNumerator*q.rightDenominator===q.rightNumerator*q.leftDenominator);assert.equal(q.answerText,q.equivalent?"是":"否");assert.deepEqual(q.metadata.requiredCapabilityIds,CAPS);assert.equal(validateG4BU08EquivalenceCrossProductQuestion(q).ok,true);}});
          test("P03F12 validator rejects cross-product, answer and scope tampering",()=>{const q=generateG4BU08EquivalenceCrossProductQuestions(plan({questionCount:1})).questions[0];assert.equal(validateG4BU08EquivalenceCrossProductQuestion({...q,leftCrossProduct:q.leftCrossProduct+1}).ok,false);assert.equal(validateG4BU08EquivalenceCrossProductQuestion({...q,answerText:"錯"}).ok,false);assert.equal(validateG4BU08EquivalenceCrossProductQuestion({...q,leftDenominator:0}).ok,false);assert.equal(validateG4BU08EquivalenceCrossProductQuestion({...q,questionMode:"application"}).ok,false);});
          test("P03F12 selector and current Pixel expose two G4B-U08 KPs without adding a source",()=>{const rows=listVisibleBatchAKnowledgePoints().filter((row)=>row.sourceId===G4B_U08_SOURCE_ID);assert.equal(rows.length,2);assert.deepEqual(rows.map((row)=>row.knowledgePointId),["kp_g4b_u08_generate_equivalent_fraction",G4B_U08_EQUIVALENCE_CROSS_PRODUCT_KP_ID]);const a=listBatchAKnowledgePointAvailabilityBySource(G4B_U08_SOURCE_ID);assert.equal(a.visibleCount,2);assert.equal(a.hiddenPendingCount,5);assert.equal(listCurrentPixelSourceOptions().length,25);assert.equal(listPixelKnowledgePointsForSource(G4B_U08_SOURCE_ID).length,2);assert.equal(getCurrentPixelRegistrySnapshot().sourceCount,25);});
          test("P03F12 shared worksheet and answer key render bounded pages",()=>{const result=buildWorksheetDocumentFromPlan(plan());assert.equal(result.ok,true,JSON.stringify(result.errors));assert.equal(result.worksheetDocument.generatedQuestions.length,8);assert.equal(result.worksheetDocument.answerKeyItems.length,8);assert.equal(result.worksheetDocument.questionPages.length,1);assert.equal(result.worksheetDocument.answerKeyPages.length,1);});
          ''',
          }

          for path, content in files.items():
              p=Path(path); p.parent.mkdir(parents=True,exist_ok=True); p.write_text(content.replace('\n          ','\n').lstrip(),encoding='utf-8')

          replacements = {
            'site/modules/curriculum/registry/batch-a-selector-extension.js': [
              ('import * as current from "./batch-a-selector-p03f11-extension.js";', 'import * as current from "./batch-a-selector-p03f12-extension.js";'),
              ('bounded P03F11 successor', 'bounded P03F12 successor')],
            'site/pixel/pixel-registry-bridge.js': [
              ('batch-a-selector-p03f11-extension.js', 'batch-a-selector-p03f12-extension.js')],
            'site/modules/curriculum/batch-a/batch-a-browser-question-router.js': [
              ('batch-a-browser-generator-p03f11.js', 'batch-a-browser-generator-p03f12.js'),
              ('import { canGenerateG4BU08EquivalentFractionQuestions, generateG4BU08EquivalentFractionQuestions } from "./equivalent-fraction-runtime.js";', 'import { canGenerateG4BU08EquivalentFractionQuestions, generateG4BU08EquivalentFractionQuestions } from "./equivalent-fraction-runtime.js";\nimport { canGenerateG4BU08EquivalenceCrossProductQuestions, generateG4BU08EquivalenceCrossProductQuestions } from "./equivalence-cross-product-runtime.js";'),
              ('  if (canGenerateG4BU08EquivalentFractionQuestions(plan)) return generateG4BU08EquivalentFractionQuestions(options);', '  if (canGenerateG4BU08EquivalenceCrossProductQuestions(plan)) return generateG4BU08EquivalenceCrossProductQuestions(options);\n  if (canGenerateG4BU08EquivalentFractionQuestions(plan)) return generateG4BU08EquivalentFractionQuestions(options);')],
            'site/modules/curriculum/batch-a/batch-a-browser-worksheet-s59j-r1-extension.js': [
              ('batch-a-browser-worksheet-p03f11-extension.js', 'batch-a-browser-worksheet-p03f12-extension.js')],
            'site/modules/curriculum/registry/full-product-public-control-profiles.js': [
              ('task: "P03F_W3DirectProductVerticalSlice005Implementation", questionTypeControl:', 'task: "P03F_W3DirectProductVerticalSlice012Implementation", questionTypeControl:'),
              ('w3_slice005_equivalent_fraction_numeric_only', 'w3_slice012_two_equivalence_kp_numeric_only'),
              ('&& options.includeW3Slice011 !== true;', '&& options.includeW3Slice011 !== true && options.includeW3Slice012 !== true;'),
              ('  const includeSlice11 = options.includeW3Slice011 === true;', '  const includeSlice12 = options.includeW3Slice012 === true;\n  const includeSlice11 = options.includeW3Slice011 === true || includeSlice12;'),
              ('w3Slice011ProfileCount: includeSlice11 ? 1 : 0, pblProfileCount:', 'w3Slice011ProfileCount: includeSlice11 ? 1 : 0, w3Slice012ProfileCount: includeSlice12 ? 1 : 0, pblProfileCount:')]
          }
          for path, pairs in replacements.items():
              p=Path(path); text=p.read_text(encoding='utf-8')
              for old,new in pairs:
                  if text.count(old)!=1: raise SystemExit(f'{path}: expected one replacement for {old!r}, got {text.count(old)}')
                  text=text.replace(old,new)
              p.write_text(text,encoding='utf-8')

          p=Path('tests/curriculum/p03f-slice005-equivalent-fraction.test.js')
          text=p.read_text(encoding='utf-8')
          marker='import {\n  getCurrentPixelRegistrySnapshot,'
          addition='import { G4B_U08_EQUIVALENCE_CROSS_PRODUCT_KP_ID } from "../../site/modules/curriculum/registry/g4b-u08-equivalence-cross-product-selector-projection.js";\n'
          if addition not in text:
              if text.count(marker)!=1: raise SystemExit('P03F5 import marker missing')
              text=text.replace(marker,addition+marker)
          old='''test("P03F5 current Pixel exposes 25 sources and one G4B-U08 KP", () => {
  const sources = listCurrentPixelSourceOptions();
  assert.equal(sources.length, 25);
  const source = sources.find((row) => row.sourceId === G4B_U08_SOURCE_ID);
  assert.ok(source);
  assert.equal(source.visibleKnowledgePointCount, 1);
  assert.equal(source.hiddenPendingCount, 6);
  assert.deepEqual(listPixelKnowledgePointsForSource(G4B_U08_SOURCE_ID).map((row) => row.knowledgePointId), [G4B_U08_EQUIVALENT_FRACTION_KP_ID]);
  const snapshot = getCurrentPixelRegistrySnapshot();
  assert.equal(snapshot.sourceCount, 25);
  assert.equal(snapshot.bySourceId[G4B_U08_SOURCE_ID].visibleKnowledgePoints.length, 1);
});'''
          new='''test("P03F5 historical authority stays one KP while current Pixel expands monotonically to two", () => {
  const sources = listCurrentPixelSourceOptions();
  assert.equal(sources.length, 25);
  const source = sources.find((row) => row.sourceId === G4B_U08_SOURCE_ID);
  assert.ok(source);
  assert.equal(source.visibleKnowledgePointCount, 2);
  assert.equal(source.hiddenPendingCount, 5);
  assert.deepEqual(listPixelKnowledgePointsForSource(G4B_U08_SOURCE_ID).map((row) => row.knowledgePointId), [G4B_U08_EQUIVALENT_FRACTION_KP_ID, G4B_U08_EQUIVALENCE_CROSS_PRODUCT_KP_ID]);
  const snapshot = getCurrentPixelRegistrySnapshot();
  assert.equal(snapshot.sourceCount, 25);
  assert.equal(snapshot.bySourceId[G4B_U08_SOURCE_ID].visibleKnowledgePoints.length, 2);
});'''
          if text.count(old)!=1: raise SystemExit('P03F5 current Pixel block mismatch')
          p.write_text(text.replace(old,new),encoding='utf-8')
          PY
      - name: Run focused core tests
        run: |
          node --test tests/curriculum/p03f-slice005-equivalent-fraction.test.js tests/curriculum/p03f-slice012-equivalence-cross-product.test.js
      - name: Commit Slice012 core
        shell: bash
        run: |
          set -euxo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add site/modules/curriculum site/pixel/pixel-registry-bridge.js tests/curriculum/p03f-slice005-equivalent-fraction.test.js tests/curriculum/p03f-slice012-equivalence-cross-product.test.js
          git diff --cached --check
          git commit -m "P03F12: connect fraction equivalence core runtime"
          git push origin HEAD:${{ github.event.pull_request.head.ref }}
