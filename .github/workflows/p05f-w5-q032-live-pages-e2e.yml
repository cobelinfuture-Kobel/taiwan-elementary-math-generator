name: P05F W5 Q032 Post-Merge Pages E2E

on:
  push:
    branches: [main]
    paths:
      - 'data/curriculum/full-product/p05f/q032-g4a-u05-triangle-inequality-implementation.json'
      - 'tools/curriculum/run-p05f-w5-q032-live-pages-e2e.mjs'
      - '.github/workflows/p05f-w5-q032-live-pages-e2e.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: p05f-w5-q032-pages-${{ github.ref }}
  cancel-in-progress: true

jobs:
  live-pages-e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install Playwright Chromium
        run: |
          npm install --no-save --package-lock=false playwright@1.55.0
          npx playwright install --with-deps chromium
      - name: Run true Q032 exact deployed Pages E2E
        env:
          P05F32_DEPLOYMENT_RETRIES: "36"
          P05F32_DEPLOYMENT_RETRY_DELAY_MS: "15000"
        run: node tools/curriculum/run-p05f-w5-q032-live-pages-e2e.mjs
      - name: Upload Q032 exact Pages evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: p05f-w5-q032-pages-e2e
          path: tmp/p05f-w5-q032-live-pages-e2e
          if-no-files-found: error
          retention-days: 30
