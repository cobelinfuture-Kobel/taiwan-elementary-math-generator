name: PGC-R01 Capability Matrix Materialization

on:
  push:
    branches:
      - pgc-r01-public-capability-matrix
    paths:
      - "tools/curriculum/materialize-pgc-r01-public-capability-matrix.mjs"
      - "tests/curriculum/pgc-r01-public-capability-matrix.test.js"
      - ".github/workflows/pgc-r01-capability-matrix-materialization.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  materialize:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: pgc-r01-public-capability-matrix
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Materialize R01 artifacts
        run: node tools/curriculum/materialize-pgc-r01-public-capability-matrix.mjs

      - name: Focused R01 acceptance
        run: node --test tests/curriculum/pgc-r01-public-capability-matrix.test.js

      - name: Commit deterministic artifacts
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            data/curriculum/public-generation/public_generation_capability_matrix.json \
            data/curriculum/public-generation/public_generation_capability_matrix.csv \
            docs/curriculum/output/PGC-R01_capability_gap_report.md
          if git diff --cached --quiet; then
            echo "Capability artifacts already synchronized."
          else
            git commit -m "data(pgc-r01): materialize public capability matrix [skip ci]"
            git push origin HEAD:pgc-r01-public-capability-matrix
          fi
