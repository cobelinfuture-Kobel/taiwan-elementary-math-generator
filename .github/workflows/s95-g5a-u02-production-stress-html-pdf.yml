name: S95 G5A-U02 Production Stress HTML PDF

on:
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  production-stress:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install project dependencies
        run: npm install

      - name: Install browser and PDF verification dependencies
        run: |
          npm install --no-save playwright@1.55.0
          npx playwright install --with-deps chromium
          sudo apt-get update
          sudo apt-get install -y fonts-noto-cjk poppler-utils
          python3 -m pip install --break-system-packages pypdf pillow

      - name: Run full Node test suite
        run: npm test

      - name: Generate 100-scenario audit and 200-question stress HTML
        run: node tools/curriculum/generate-s95-g5a-u02-production-stress.mjs

      - name: Verify DOM containment and print stress PDF
        run: |
          node --input-type=module <<'NODE'
          import { chromium } from 'playwright';
          import { pathToFileURL } from 'node:url';
          import { resolve } from 'node:path';
          import { readFile } from 'node:fs/promises';
          const root = resolve('docs/curriculum/output/stress');
          const htmlPath = resolve(root, 'S95_G5A_U02_ProductionStress200.html');
          const pdfPath = resolve(root, 'S95_G5A_U02_ProductionStress200.pdf');
          const manifest = JSON.parse(await readFile(resolve(root, 'S95_G5A_U02_ProductionStress.manifest.json'), 'utf8'));
          const browser = await chromium.launch({ headless: true });
          const page = await browser.newPage({ viewport: { width: 1280, height: 960 } });
          await page.goto(pathToFileURL(htmlPath).href, { waitUntil: 'networkidle' });
          const containment = await page.evaluate(() => {
            const cards = [...document.querySelectorAll('.g5a-u02-card')];
            const overflow = cards.filter((node) => node.scrollHeight > node.clientHeight + 1 || node.scrollWidth > node.clientWidth + 1);
            return {
              questionCardCount: document.querySelectorAll('.g5a-u02-card--question').length,
              answerCardCount: document.querySelectorAll('.g5a-u02-card--answer').length,
              pageCount: document.querySelectorAll('.g5a-u02-page').length,
              overflowCount: overflow.length,
              marker: document.body.dataset.s95ProductionStress ?? null,
            };
          });
          console.log(JSON.stringify(containment, null, 2));
          if (containment.questionCardCount !== 200) throw new Error(`question cards=${containment.questionCardCount}`);
          if (containment.answerCardCount !== 200) throw new Error(`answer cards=${containment.answerCardCount}`);
          if (containment.pageCount !== manifest.expectedPdfPageCount) throw new Error(`HTML pages=${containment.pageCount}/${manifest.expectedPdfPageCount}`);
          if (containment.overflowCount !== 0) throw new Error(`DOM overflow=${containment.overflowCount}`);
          if (containment.marker !== 'true') throw new Error('S95 marker missing');
          await page.emulateMedia({ media: 'print' });
          await page.pdf({ path: pdfPath, format: 'A4', printBackground: true, preferCSSPageSize: true, margin: { top: '0mm', right: '0mm', bottom: '0mm', left: '0mm' } });
          await browser.close();
          NODE

      - name: Verify all PDF pages nonblank and contained
        run: |
          rm -rf /tmp/s95-rendered && mkdir -p /tmp/s95-rendered
          pdftoppm -png -r 100 docs/curriculum/output/stress/S95_G5A_U02_ProductionStress200.pdf /tmp/s95-rendered/page
          pdftotext -bbox-layout docs/curriculum/output/stress/S95_G5A_U02_ProductionStress200.pdf /tmp/s95-bbox.html
          python3 <<'PY'
          import json, re
          from pathlib import Path
          from PIL import Image, ImageStat
          from pypdf import PdfReader
          root = Path('docs/curriculum/output/stress')
          manifest = json.loads((root / 'S95_G5A_U02_ProductionStress.manifest.json').read_text(encoding='utf-8'))
          reader = PdfReader(str(root / 'S95_G5A_U02_ProductionStress200.pdf'))
          images = sorted(Path('/tmp/s95-rendered').glob('page-*.png'))
          expected = manifest['expectedPdfPageCount']
          if len(reader.pages) != expected or len(images) != expected:
              raise SystemExit(f'page mismatch pdf={len(reader.pages)} png={len(images)} expected={expected}')
          for image_path in images:
              stat = ImageStat.Stat(Image.open(image_path).convert('L'))
              if stat.extrema[0][0] >= 245 or stat.var[0] <= 5:
                  raise SystemExit(f'blank page: {image_path}')
          text = Path('/tmp/s95-bbox.html').read_text(encoding='utf-8', errors='ignore')
          page_re = re.compile(r'<page[^>]*width="([0-9.]+)"[^>]*height="([0-9.]+)"[^>]*>(.*?)</page>', re.S)
          word_re = re.compile(r'<word[^>]*xMin="([0-9.]+)"[^>]*yMin="([0-9.]+)"[^>]*xMax="([0-9.]+)"[^>]*yMax="([0-9.]+)"', re.S)
          pages = page_re.findall(text)
          overflow = []
          for index, (width, height, body) in enumerate(pages, 1):
              width, height = float(width), float(height)
              for coords in word_re.findall(body):
                  x0, y0, x1, y1 = map(float, coords)
                  if x0 < -0.5 or y0 < -0.5 or x1 > width + 0.5 or y1 > height + 0.5:
                      overflow.append((index, x0, y0, x1, y1))
          if overflow:
              raise SystemExit(f'bbox overflow count={len(overflow)} first={overflow[0]}')
          if not (reader.pages[-1].extract_text() or '').strip():
              raise SystemExit('final answer page blank')
          print(f'S95 PDF verification PASS: {expected} pages')
          PY

      - name: Upload S95 stress artifacts
        uses: actions/upload-artifact@v4
        with:
          name: s95-g5a-u02-production-stress-html-pdf
          if-no-files-found: error
          retention-days: 30
          path: |
            docs/curriculum/output/stress/S95_G5A_U02_ProductionStress200.html
            docs/curriculum/output/stress/S95_G5A_U02_ProductionStress200.pdf
            docs/curriculum/output/stress/S95_G5A_U02_ProductionStress.manifest.json
