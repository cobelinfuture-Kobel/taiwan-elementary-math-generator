name: P03F5 Slice005 Product Acceptance

on:
  workflow_dispatch:
permissions:
  contents: read

concurrency:
  group: p03f5-slice005-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  chromium:
    if: github.head_ref == 'p03f-w3-direct-product-vertical-slice005-v1'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install dependencies and Chromium
        run: |
          npm install
          npm install --no-save --package-lock=false playwright@1.51.1
          npx playwright install --with-deps chromium
      - name: Validate slice005 product admission
        run: node tools/curriculum/validate-p03f-slice005-product-admission.mjs
      - name: Render slice005 Chromium acceptance
        run: node tools/curriculum/render-p03f-slice005-product-acceptance.mjs
      - name: Upload slice005 product artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: p03f-slice005-product-acceptance
          path: tmp/p03f-slice005-product-acceptance
          if-no-files-found: warn
          retention-days: 30

