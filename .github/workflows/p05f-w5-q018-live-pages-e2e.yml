name: P05F W5 Q018 Post-Merge Pages E2E

on:
  push:
    branches: [main]
    paths:
      - 'data/curriculum/full-product/p05f/q018-g5a-u10a1-edge-length-face-relationship-implementation.json'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: p05f-w5-q018-pages-${{ github.ref }}
  cancel-in-progress: true

jobs:
  live-pages-e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install Playwright Chromium
        run: |
          npm install --no-save --package-lock=false playwright@1.55.0
          npx playwright install --with-deps chromium
      - name: Run true Q018 exact deployed Pages E2E
        env:
          P05F18_DEPLOYMENT_RETRIES: "36"
          P05F18_DEPLOYMENT_RETRY_DELAY_MS: "15000"
        run: node tools/curriculum/run-p05f-w5-q018-live-pages-e2e.mjs
      - name: Upload Q018 exact Pages evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: p05f-w5-q018-pages-e2e
          path: tmp/p05f-w5-q018-live-pages-e2e
          if-no-files-found: error
          retention-days: 30
