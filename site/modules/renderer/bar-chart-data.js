function escapeHtml(value){return String(value).replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#39;");}
export function validateBarChartData(model){if(!model||model.kind!=="bar_chart_data")return false;if(typeof model.title!=="string"||typeof model.xAxisLabel!=="string"||typeof model.yAxisLabel!=="string")return false;if(!Number.isInteger(model.scaleStep)||model.scaleStep<=0||!Number.isInteger(model.tickCount)||model.tickCount<2||model.tickCount>8||model.maxValue!==model.scaleStep*model.tickCount)return false;if(!Array.isArray(model.bars)||model.bars.length<2||model.bars.length>6)return false;if(!["BAR_CHART_VALUE_READING","CHART_SCALE_INTERPRETATION"].includes(model.semanticCore))return false;return model.bars.every(b=>b&&typeof b.label==="string"&&Number.isInteger(b.value)&&b.value>=0&&b.value<=model.maxValue&&b.value%model.scaleStep===0);}
export function renderBarChartData(model){if(!validateBarChartData(model)){const error=new Error("Bar chart representation is invalid.");error.code="bar_chart_data_invalid";throw error;}const width=360,height=190,left=48,right=342,top=28,bottom=150,plotW=right-left,plotH=bottom-top,n=model.bars.length,slot=plotW/n,barW=Math.min(42,slot*.56);const y=v=>bottom-(v/model.maxValue)*plotH;const grid=Array.from({length:model.tickCount+1},(_,i)=>{const value=i*model.scaleStep,yy=y(value).toFixed(2);return `<g class="worksheet-bar-chart__tick"><line x1="${left}" y1="${yy}" x2="${right}" y2="${yy}" stroke="currentColor" stroke-opacity=".25" stroke-width="1"/><text x="${left-7}" y="${(Number(yy)+3).toFixed(2)}" text-anchor="end" font-size="9">${escapeHtml(value)}</text></g>`;}).join("");const bars=model.bars.map((b,i)=>{const cx=left+slot*(i+.5),yy=y(b.value),h=bottom-yy,x=cx-barW/2;return `<g class="worksheet-bar-chart__bar" data-bar-index="${i}"><rect x="${x.toFixed(2)}" y="${yy.toFixed(2)}" width="${barW.toFixed(2)}" height="${h.toFixed(2)}" fill="currentColor" fill-opacity=".68"/><text x="${cx.toFixed(2)}" y="${bottom+14}" text-anchor="middle" font-size="10">${escapeHtml(b.label)}</text></g>`;}).join("");return ['<div class="worksheet-cell__representation worksheet-cell__representation--chart" data-representation="bar-chart">',`<svg class="worksheet-bar-chart" data-scale-step="${model.scaleStep}" data-semantic-core="${escapeHtml(model.semanticCore)}" viewBox="0 0 ${width} ${height}" role="img" aria-label="${escapeHtml(model.ariaLabel??model.title)}" preserveAspectRatio="xMidYMid meet" style="display:block;width:100%;max-width:360px;height:auto;margin:.2rem auto;">`,`<title>${escapeHtml(model.title)}</title>`,`<text x="${width/2}" y="14" text-anchor="middle" font-size="11" font-weight="700">${escapeHtml(model.title)}</text>`,grid,`<line x1="${left}" y1="${top}" x2="${left}" y2="${bottom}" stroke="currentColor" stroke-width="1.5"/><line x1="${left}" y1="${bottom}" x2="${right}" y2="${bottom}" stroke="currentColor" stroke-width="1.5"/>`,bars,`<text x="12" y="${(top+bottom)/2}" text-anchor="middle" font-size="9" transform="rotate(-90 12 ${(top+bottom)/2})">${escapeHtml(model.yAxisLabel)}</text>`,`<text x="${(left+right)/2}" y="184" text-anchor="middle" font-size="9">${escapeHtml(model.xAxisLabel)}</text>`,"</svg>","</div>"].join("");}
