export function validateAreaConservationIrregularGridDiagramModel(model){const errors=[];if(!model||model.kind!=="area_conservation_irregular_grid_diagram")errors.push("P05F23_AREA_KIND_INVALID");if(!Number.isInteger(model?.variant)||model.variant<0||model.variant>=240)errors.push("P05F23_AREA_VARIANT_INVALID");if(typeof model?.relation!=="string"||!model.relation)errors.push("P05F23_AREA_RELATION_INVALID");if(model?.diagramMode==="AREA_CONSERVATION_REARRANGE"){if(!["CUT_TRANSLATE","CUT_REARRANGE"].includes(model.transformationMode)||!Number.isFinite(model.areaBeforeCm2)||model.areaBeforeCm2<=0||model.areaAfterCm2!==model.areaBeforeCm2||!Number.isInteger(model.pieceCount)||model.pieceCount<2||model.pieceCount>4||![0,1,2,3].includes(model.orientation)||!Number.isInteger(model.shift)||model.shift<1||model.noAddition!==true||model.noOverlap!==true||model.noLoss!==true)errors.push("P05F23_AREA_CONSERVATION_INVALID");}else if(["IRREGULAR_GRID_DECOMPOSE","IRREGULAR_GRID_COMPLETE","IRREGULAR_GRID_PARTIAL_CELLS"].includes(model?.diagramMode)){if(!Number.isInteger(model.cols)||model.cols<5||model.cols>7||!Number.isInteger(model.rows)||model.rows<4||model.rows>6||!Number.isInteger(model.wholeCount)||model.wholeCount<0||!Number.isInteger(model.halfCount)||model.halfCount<0||model.halfCount%2!==0||!Number.isInteger(model.quarterCount)||model.quarterCount<0||model.quarterCount%4!==0||model.wholeCount+model.halfCount+model.quarterCount>model.cols*model.rows||model.areaCm2!==model.wholeCount+model.halfCount/2+model.quarterCount/4||model.unitAreaCm2!==1||model.noOverlap!==true||model.noLoss!==true)errors.push("P05F23_IRREGULAR_GRID_INVALID");if(model.diagramMode==="IRREGULAR_GRID_COMPLETE"&&(model.completionTotalCm2!==model.cols*model.rows||model.missingAreaCm2!==model.completionTotalCm2-model.areaCm2))errors.push("P05F23_IRREGULAR_COMPLETION_INVALID");}else errors.push("P05F23_AREA_MODE_INVALID");return Object.freeze({ok:errors.length===0,errors:Object.freeze(errors)});}
function fixed(v){return Number(v).toFixed(2);}function escapeHtml(value){return String(value).replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#39;");}
function renderConservation(model){const baseX=18,baseY=30,w=74,h=54,gap=116,pieceW=w/model.pieceCount,pieces=[];for(let i=0;i<model.pieceCount;i+=1){const x=baseX+i*pieceW,y=baseY;pieces.push(`<rect x="${fixed(x)}" y="${fixed(y)}" width="${fixed(pieceW)}" height="${h}" fill="currentColor" opacity="${0.12+0.05*(i%3)}" stroke="currentColor" stroke-width="1"/>`);const tx=gap+baseX+(i%2)*pieceW+model.shift*2,ty=baseY+(i%2)*12+(model.orientation%2)*4;pieces.push(`<rect x="${fixed(tx)}" y="${fixed(ty)}" width="${fixed(pieceW)}" height="${h}" fill="currentColor" opacity="${0.12+0.05*(i%3)}" stroke="currentColor" stroke-width="1"/>`);}return [`<div class="worksheet-cell__representation worksheet-cell__representation--area-reasoning" data-representation="area-conservation-irregular-grid-diagram" data-diagram-mode="${escapeHtml(model.diagramMode)}" data-relation="${escapeHtml(model.relation)}" data-variant="${model.variant}">`,'<svg viewBox="0 0 230 130" width="100%" height="118" role="img" aria-label="剪拼後面積保持不變示意圖" preserveAspectRatio="xMidYMid meet">',...pieces,`<text x="55" y="104" text-anchor="middle" font-size="10">剪拼前 ${escapeHtml(model.areaBeforeCm2)} cm²</text>`,`<text x="174" y="104" text-anchor="middle" font-size="10">重組後</text>`,`<text x="114" y="120" text-anchor="middle" font-size="9">沒有增加、重疊或遺失</text>`,`</svg></div>`].join("");}
function renderIrregular(model){const cell=15,width=model.cols*cell,height=model.rows*cell,left=(230-width)/2,top=12,marks=[],grid=[];let index=0;for(let i=0;i<model.wholeCount;i+=1,index+=1){const c=index%model.cols,r=Math.floor(index/model.cols),x=left+c*cell,y=top+r*cell;marks.push(`<rect x="${fixed(x)}" y="${fixed(y)}" width="${cell}" height="${cell}" fill="currentColor" opacity="0.18"/>`);}for(let i=0;i<model.halfCount;i+=1,index+=1){const c=index%model.cols,r=Math.floor(index/model.cols),x=left+c*cell,y=top+r*cell,pts=i%2===0?`${fixed(x)},${fixed(y)} ${fixed(x+cell)},${fixed(y)} ${fixed(x)},${fixed(y+cell)}`:`${fixed(x+cell)},${fixed(y+cell)} ${fixed(x+cell)},${fixed(y)} ${fixed(x)},${fixed(y+cell)}`;marks.push(`<polygon points="${pts}" fill="currentColor" opacity="0.28"/>`);}for(let i=0;i<model.quarterCount;i+=1,index+=1){const c=index%model.cols,r=Math.floor(index/model.cols),x=left+c*cell,y=top+r*cell,half=cell/2;marks.push(`<rect x="${fixed(x)}" y="${fixed(y)}" width="${fixed(half)}" height="${fixed(half)}" fill="currentColor" opacity="0.35"/>`);}for(let c=0;c<=model.cols;c+=1){const x=left+c*cell;grid.push(`<line x1="${fixed(x)}" y1="${fixed(top)}" x2="${fixed(x)}" y2="${fixed(top+height)}" stroke="currentColor" stroke-width="1"/>`);}for(let r=0;r<=model.rows;r+=1){const y=top+r*cell;grid.push(`<line x1="${fixed(left)}" y1="${fixed(y)}" x2="${fixed(left+width)}" y2="${fixed(y)}" stroke="currentColor" stroke-width="1"/>`);}const completion=model.diagramMode==="IRREGULAR_GRID_COMPLETE"?`<rect x="${fixed(left)}" y="${fixed(top)}" width="${fixed(width)}" height="${fixed(height)}" fill="none" stroke="currentColor" stroke-width="2" stroke-dasharray="4 3"/>`:"";return [`<div class="worksheet-cell__representation worksheet-cell__representation--area-reasoning" data-representation="area-conservation-irregular-grid-diagram" data-diagram-mode="${escapeHtml(model.diagramMode)}" data-relation="${escapeHtml(model.relation)}" data-variant="${model.variant}">`,'<svg viewBox="0 0 230 130" width="100%" height="118" role="img" aria-label="不規則方格面積示意圖" preserveAspectRatio="xMidYMid meet">',...marks,...grid,completion,`<text x="115" y="120" text-anchor="middle" font-size="10">每1小格 = 1 cm²</text>`,`</svg></div>`].join("");}
export function renderAreaConservationIrregularGridDiagram(model){const validation=validateAreaConservationIrregularGridDiagramModel(model);if(!validation.ok){const error=new Error(`Q023 area reasoning diagram invalid: ${validation.errors.join(",")}`);error.code="p05f23_area_reasoning_diagram_invalid";throw error;}return model.diagramMode==="AREA_CONSERVATION_REARRANGE"?renderConservation(model):renderIrregular(model);}
