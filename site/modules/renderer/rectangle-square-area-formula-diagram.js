const MODES=new Set(["RECTANGLE_DIMENSIONS","SQUARE_DIMENSIONS","UNIT_GRID_ROW_COLUMN_PRODUCT"]);
const esc=value=>String(value).replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;");
function valid(model){if(!model||model.kind!=="rectangle_square_area_formula_diagram"||!MODES.has(model.diagramMode)||!Number.isInteger(model.variant)||model.variant<0||model.variant>=240||model.unit!=="cm"||model.unitAreaCm2!==1||!Number.isInteger(model.areaCm2)||model.areaCm2<=0)return false;if(model.diagramMode==="RECTANGLE_DIMENSIONS")return model.shapeType==="RECTANGLE"&&Number.isInteger(model.lengthCm)&&Number.isInteger(model.widthCm)&&model.lengthCm>model.widthCm&&model.areaCm2===model.lengthCm*model.widthCm&&["LANDSCAPE","PORTRAIT"].includes(model.orientation);if(model.diagramMode==="SQUARE_DIMENSIONS")return model.shapeType==="SQUARE"&&Number.isInteger(model.sideCm)&&model.sideCm>=2&&model.lengthCm===model.sideCm&&model.widthCm===model.sideCm&&model.areaCm2===model.sideCm*model.sideCm&&["TOP","RIGHT","BOTTOM","LEFT"].includes(model.labelPlacement)&&[0,1].includes(model.diagramScaleIndex);return model.shapeType==="UNIT_GRID"&&Number.isInteger(model.rows)&&Number.isInteger(model.columns)&&model.rows>=2&&model.columns>=3&&model.areaCm2===model.rows*model.columns&&model.lengthCm===model.columns&&model.widthCm===model.rows&&["LANDSCAPE","PORTRAIT"].includes(model.orientation);}
function rectangleMarkup(model){const portrait=model.orientation==="PORTRAIT",w=portrait?94:154,h=portrait?154:94,x=(300-w)/2,y=(178-h)/2;return [`<rect x="${x}" y="${y}" width="${w}" height="${h}" fill="none" stroke="currentColor" stroke-width="3"/>`,`<text x="150" y="${y+h+24}" text-anchor="middle" font-size="14">長 ${model.lengthCm} 公分</text>`,`<text x="${x-14}" y="${y+h/2}" text-anchor="middle" font-size="14" transform="rotate(-90 ${x-14} ${y+h/2})">寬 ${model.widthCm} 公分</text>`].join("");}
function squareMarkup(model){const size=model.diagramScaleIndex===0?104:124,x=(300-size)/2,y=(178-size)/2,label=`邊長 ${model.sideCm} 公分`;let text=`<text x="150" y="${y-12}" text-anchor="middle" font-size="14">${label}</text>`;if(model.labelPlacement==="BOTTOM")text=`<text x="150" y="${y+size+22}" text-anchor="middle" font-size="14">${label}</text>`;if(model.labelPlacement==="LEFT")text=`<text x="${x-14}" y="89" text-anchor="middle" font-size="14" transform="rotate(-90 ${x-14} 89)">${label}</text>`;if(model.labelPlacement==="RIGHT")text=`<text x="${x+size+14}" y="89" text-anchor="middle" font-size="14" transform="rotate(90 ${x+size+14} 89)">${label}</text>`;return `<rect x="${x}" y="${y}" width="${size}" height="${size}" fill="none" stroke="currentColor" stroke-width="3"/>${text}`;}
function gridMarkup(model){const maxW=180,maxH=116,cell=Math.min(maxW/model.columns,maxH/model.rows),w=cell*model.columns,h=cell*model.rows,x=(300-w)/2,y=(178-h)/2;const vertical=Array.from({length:model.columns+1},(_,i)=>`<line x1="${x+i*cell}" y1="${y}" x2="${x+i*cell}" y2="${y+h}" stroke="currentColor" stroke-width="1"/>`).join("");const horizontal=Array.from({length:model.rows+1},(_,i)=>`<line x1="${x}" y1="${y+i*cell}" x2="${x+w}" y2="${y+i*cell}" stroke="currentColor" stroke-width="1"/>`).join("");return `${vertical}${horizontal}<text x="150" y="${y+h+22}" text-anchor="middle" font-size="13">每排 ${model.columns} 格</text><text x="${x-12}" y="${y+h/2}" text-anchor="middle" font-size="13" transform="rotate(-90 ${x-12} ${y+h/2})">${model.rows} 排</text>`;}
export function renderRectangleSquareAreaFormulaDiagram(model){if(!valid(model)){const error=new Error("Rectangle/square area-formula diagram representation is invalid.");error.code="rectangle_square_area_formula_diagram_invalid";throw error;}const body=model.diagramMode==="RECTANGLE_DIMENSIONS"?rectangleMarkup(model):model.diagramMode==="SQUARE_DIMENSIONS"?squareMarkup(model):gridMarkup(model);return [`<div class="worksheet-cell__representation worksheet-cell__representation--rectangle-square-area-formula" data-representation="rectangle-square-area-formula-diagram" data-diagram-mode="${esc(model.diagramMode)}" data-shape-type="${esc(model.shapeType)}">`,`<svg class="worksheet-rectangle-square-area-formula-diagram" viewBox="0 0 300 178" width="100%" height="132" role="img" aria-label="長方形或正方形面積公式圖" preserveAspectRatio="xMidYMid meet">`,body,`</svg>`,`</div>`].join("");}
