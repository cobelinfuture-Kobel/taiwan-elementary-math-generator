const ROTATIONS=new Set(Array.from({length:24},(_,index)=>index*15));
const TARGET_CLASSES=new Set(["ACUTE","RIGHT","OBTUSE"]),SHAPE_CLASSES=new Set(["RECTANGLE","SQUARE"]),PROPERTY_MODES=new Set(["COUNT_FOUR_RIGHT_ANGLES","PRESERVE_UNDER_ROTATION_OR_SIDE_LENGTH_CHANGE"]);
function classify(opening){return opening<90?"ACUTE":opening===90?"RIGHT":opening<180?"OBTUSE":"INVALID";}
function valid(model){if(!model||model.kind!=="angle_properties_diagram"||!ROTATIONS.has(model.rotationDeg)||!Number.isInteger(model.variant)||model.variant<0||model.variant>239)return false;if(model.diagramMode==="ANGLE_CLASSIFICATION")return TARGET_CLASSES.has(model.targetClass)&&Number.isFinite(model.openingDeg)&&model.openingDeg>0&&model.openingDeg<180&&classify(model.openingDeg)===model.targetClass&&Number.isFinite(model.sideLength1)&&model.sideLength1>=40&&model.sideLength1<=80&&Number.isFinite(model.sideLength2)&&model.sideLength2>=40&&model.sideLength2<=70&&model.benchmarkVisible===true;if(model.diagramMode==="RECTANGLE_SQUARE_RIGHT_ANGLES"){if(!SHAPE_CLASSES.has(model.shapeClass)||!PROPERTY_MODES.has(model.propertyMode)||model.fourRightAngles!==true||!Number.isFinite(model.width)||!Number.isFinite(model.height)||model.width<40||model.height<35)return false;if(model.shapeClass==="SQUARE"&&model.width!==model.height)return false;if(model.shapeClass==="RECTANGLE"&&model.width===model.height)return false;return true;}return false;}
function point(length,angleDeg){const rad=angleDeg*Math.PI/180;return{x:(Math.cos(rad)*length).toFixed(2),y:(-Math.sin(rad)*length).toFixed(2)};}
function renderClassification(model){const second=point(model.sideLength2,model.openingDeg);return [`<g class="angle-properties-diagram__target" transform="translate(82 82) rotate(${model.rotationDeg})"><line x1="0" y1="0" x2="${model.sideLength1}" y2="0" stroke="currentColor" stroke-width="3"/><line x1="0" y1="0" x2="${second.x}" y2="${second.y}" stroke="currentColor" stroke-width="3"/><circle cx="0" cy="0" r="3" fill="currentColor"/></g>`,`<g class="angle-properties-diagram__benchmark" transform="translate(178 52)"><path d="M 0 38 L 0 0 L 38 0" fill="none" stroke="currentColor" stroke-width="3"/><path d="M 0 12 L 12 12 L 12 0" fill="none" stroke="currentColor" stroke-width="2"/><text x="19" y="56" text-anchor="middle" font-size="11">直角基準</text></g>`].join("");}
function renderShape(model){const x=(-model.width/2).toFixed(2),y=(-model.height/2).toFixed(2);return [`<g class="angle-properties-diagram__shape" transform="translate(120 66) rotate(${model.rotationDeg})"><rect x="${x}" y="${y}" width="${model.width}" height="${model.height}" fill="none" stroke="currentColor" stroke-width="3"/></g>`,`<text x="120" y="124" text-anchor="middle" font-size="11">${model.shapeClass==="SQUARE"?"正方形":"長方形"}</text>`].join("");}
export function renderAnglePropertiesDiagram(model){if(!valid(model)){const error=new Error("Angle-properties diagram representation is invalid.");error.code="angle_properties_diagram_invalid";throw error;}const body=model.diagramMode==="ANGLE_CLASSIFICATION"?renderClassification(model):renderShape(model);return [`<div class="worksheet-cell__representation worksheet-cell__representation--angle-properties" data-representation="angle-properties-diagram" data-diagram-mode="${model.diagramMode}"${model.targetClass?` data-target-class="${model.targetClass}"`:""}${model.shapeClass?` data-shape-class="${model.shapeClass}"`:""}>`,`<svg class="worksheet-angle-properties-diagram" viewBox="0 0 240 135" width="100%" height="115" role="img" aria-label="角與圖形直角性質圖示" preserveAspectRatio="xMidYMid meet">`,body,"</svg>","</div>"].join("");}
