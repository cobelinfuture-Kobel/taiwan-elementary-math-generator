import { paginateAnswerKeyItems, paginateQuestionDisplayModels } from "../../core/index.js";
import { buildBatchABrowserWorksheetDocument as buildBase } from "./batch-a-browser-worksheet-p03f6-extension.js";
import { buildBatchABrowserPlan, requestsP03F7FractionUnitConversion } from "./batch-a-browser-generator-p03f7.js";
import { generateBatchABrowserQuestions } from "./batch-a-browser-question-router.js";
import { validateBatchABrowserPlan, validateBatchABrowserQuestions } from "./batch-a-browser-validator-p03f7.js";
import { G3B_U07_SOURCE_ID } from "../registry/g3b-u07-quotient-fraction-selector-projection.js";
import { G3B_U07_FRACTION_UNIT_CONVERSION_KP_ID } from "../registry/g3b-u07-fraction-unit-conversion-selector-projection.js";
import { buildG3BU07InlineMathModel } from "./g3b-u07-inline-fraction-display.js";
export const P03F7_WORKSHEET_ADAPTER = Object.freeze({ task: "P03F_W3DirectProductVerticalSlice007Implementation", status: "bounded_shared_numeric_application_worksheet_adapter_connected", sourceId: G3B_U07_SOURCE_ID, patternSpecCount: 4, sharedPagination: true, sharedRenderer: true, parallelPipeline: false });
const failed = (errors, warnings = [], details = {}) => Object.freeze({ ok: false, errors: Object.freeze([...(errors ?? [])]), warnings: Object.freeze([...(warnings ?? [])]), worksheetDocument: null, ...details });
function layout(options, mode) { const r = options.printLayout ?? {}; return Object.freeze({ paperSize: r.paperSize ?? "A4", columns: Math.min(Number.isInteger(r.columns) ? r.columns : 2, 2), rowsPerPage: Math.min(Number.isInteger(r.rowsPerPage) ? r.rowsPerPage : 3, 3), showQuestionNumbers: r.showQuestionNumbers !== false, showAnswerKeyPage: options.includeAnswerKey !== false && r.showAnswerKeyPage !== false, longTextCardPolicy: "avoidSplit", questionMode: mode }); }
function model(q, i, show) { const prompt = q.blankedDisplayText ?? q.promptText ?? ""; return Object.freeze({ questionId: q.id, questionNumber: i + 1, patternId: q.patternSpecId, knowledgePointId: G3B_U07_FRACTION_UNIT_CONVERSION_KP_ID, patternGroupId: q.metadata.patternGroupId, questionNumberText: show ? `${i + 1}.` : null, promptText: prompt, displayText: q.displayText, blankedDisplayText: prompt, answerText: q.answerText, promptInlineMath: buildG3BU07InlineMathModel({ sourceId: G3B_U07_SOURCE_ID, plainText: prompt }), metadataSnapshot: Object.freeze({ ...q.metadata, globalContextProduction: q.globalContextProduction }), layoutHints: Object.freeze({ estimatedTextLength: String(prompt).length, hasGrouping: false, avoidPageBreakInside: true, representation: q.questionMode === "application" ? "fraction_unit_conversion_application" : "fraction_unit_conversion_numeric", longTextCardPolicy: "avoidSplit" }) }); }
export function buildBatchABrowserWorksheetDocument(options = {}) {
  if (!requestsP03F7FractionUnitConversion(options)) return buildBase(options);
  const plan = buildBatchABrowserPlan(options); const planValidation = validateBatchABrowserPlan(plan);
  if (!planValidation.ok) return failed(planValidation.errors, planValidation.warnings, { plan, validation: planValidation });
  const generation = generateBatchABrowserQuestions(options); if (!generation.ok) return failed(generation.errors, generation.warnings, { plan, generation });
  const validation = validateBatchABrowserQuestions(generation.questions); if (!validation.ok) return failed(validation.errors, validation.warnings, { plan, generation, validation });
  const printLayout = layout(options, plan.questionMode); const models = generation.questions.map((q, i) => model(q, i, printLayout.showQuestionNumbers));
  const answers = printLayout.showAnswerKeyPage ? generation.questions.map((q, i) => Object.freeze({ questionId: q.id, questionNumber: i + 1, patternId: q.patternSpecId, knowledgePointId: G3B_U07_FRACTION_UNIT_CONVERSION_KP_ID, patternGroupId: q.metadata.patternGroupId, promptText: models[i].blankedDisplayText, answerText: q.answerText, promptInlineMath: buildG3BU07InlineMathModel({ sourceId: G3B_U07_SOURCE_ID, plainText: models[i].blankedDisplayText }), answerInlineMath: buildG3BU07InlineMathModel({ sourceId: G3B_U07_SOURCE_ID, plainText: q.answerText }), metadataSnapshot: models[i].metadataSnapshot, layoutHints: Object.freeze({ avoidPageBreakInside: true, representation: "fraction_unit_conversion_answer" }) })) : [];
  const questionPages = paginateQuestionDisplayModels(models, printLayout); const answerKeyPages = printLayout.showAnswerKeyPage ? paginateAnswerKeyItems(answers, { ...printLayout, columns: 2, rowsPerPage: 3 }) : [];
  const document = Object.freeze({ schemaVersion: "worksheet-document-v1", version: "1", worksheetId: `p03f7-${plan.questionMode}-${plan.questionCount}-${plan.generationSeed}`, worksheetKind: "batchAWorksheet", title: options.title ?? `三年級｜單位分數與離散單位換算｜${plan.questionMode === "application" ? "應用題" : "數字題"}`, subtitle: "依每大單位所含個數，在分數單位與個數間換算", generatedAt: "DETERMINISTIC", configSnapshot: Object.freeze({ ...plan, printLayout }), orderingMode: plan.ordering, questionCount: generation.questions.length, questionPages: Object.freeze(questionPages), answerKeyPages: Object.freeze(answerKeyPages), sections: Object.freeze([]), generatedQuestions: Object.freeze(generation.questions), questions: Object.freeze(generation.questions), questionDisplayModels: Object.freeze(models), answerKeyItems: Object.freeze(answers), printOptions: Object.freeze({ ...printLayout, answerKeyColumns: 2, answerKeyRowsPerPage: 3, showAnswerKey: printLayout.showAnswerKeyPage, answerKeyPlacement: printLayout.showAnswerKeyPage ? "afterQuestions" : "none" }), publicControls: Object.freeze({ sourceId: G3B_U07_SOURCE_ID, questionMode: plan.questionMode, productAdmissionTask: P03F7_WORKSHEET_ADAPTER.task, globalContextRegistry: plan.questionMode === "application" ? "W02_ATOMIC_CONTEXT_BINDING" : null }), metadata: Object.freeze({ sourceId: G3B_U07_SOURCE_ID, knowledgePointId: G3B_U07_FRACTION_UNIT_CONVERSION_KP_ID, applicationClassification: "APPLICATION_REQUIRED", worksheetAdapter: P03F7_WORKSHEET_ADAPTER }), batchA: Object.freeze({ sourceId: G3B_U07_SOURCE_ID, questionMode: plan.questionMode, selectionMode: plan.selectionMode }), report: Object.freeze({ ok: true, errors: Object.freeze([]), warnings: Object.freeze([]), summary: Object.freeze({ questionCount: generation.questions.length, questionPageCount: questionPages.length, answerKeyPageCount: answerKeyPages.length }) }), summary: Object.freeze({ questionCount: generation.questions.length, questionPageCount: questionPages.length, answerKeyPageCount: answerKeyPages.length, numericQuestionCount: plan.questionMode === "numeric" ? generation.questions.length : 0, applicationQuestionCount: plan.questionMode === "application" ? generation.questions.length : 0 }) });
  return Object.freeze({ ok: true, errors: Object.freeze([]), warnings: Object.freeze([]), worksheetDocument: document, plan, generation, validation, p03f7WorksheetAdapter: P03F7_WORKSHEET_ADAPTER });
}
