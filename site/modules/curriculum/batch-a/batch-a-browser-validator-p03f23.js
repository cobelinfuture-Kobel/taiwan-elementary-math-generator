export * from "./batch-a-browser-validator-p03f22.js";
import { validateBatchABrowserPlan as basePlan, validateBatchABrowserQuestion as baseQuestion, validateBatchABrowserQuestions as baseQuestions } from "./batch-a-browser-validator-p03f22.js";
import { G6A_U02_SOURCE_ID, G6A_U02_RECIPROCAL_SPEC_IDS } from "../registry/g6a-u02-reciprocal-selector-projection.js";
import { validateG6AU02Slice023Question } from "./g6a-u02-reciprocal-runtime-p03f23.js";
const id = (question) => question.patternSpecId ?? question.metadata?.patternId; const isCurrent = (question) => G6A_U02_RECIPROCAL_SPEC_IDS.includes(id(question)); const issue = (code, path) => ({ code, severity: "error", path, message: code });
export function validateBatchABrowserPlan(plan = {}) { if (plan.sourceId !== G6A_U02_SOURCE_ID || !plan.patternSpecIds?.some((patternId) => G6A_U02_RECIPROCAL_SPEC_IDS.includes(patternId))) return basePlan(plan); const errors = []; if (JSON.stringify(plan.patternSpecIds) !== JSON.stringify([...G6A_U02_RECIPROCAL_SPEC_IDS])) errors.push(issue("p03f23_pattern_set_invalid", "patternSpecIds")); if (!Number.isInteger(plan.questionCount) || plan.questionCount < 1 || plan.questionCount > 240) errors.push(issue("p03f23_question_count_invalid", "questionCount")); if (plan.questionMode !== "numeric") errors.push(issue("p03f23_question_mode_invalid", "questionMode")); if (plan.genericFallbackAllowed !== false) errors.push(issue("p03f23_generic_fallback_must_be_disabled", "genericFallbackAllowed")); return { ok: errors.length === 0, errors, warnings: [] }; }
export function validateBatchABrowserQuestion(question = {}) { return isCurrent(question) ? validateG6AU02Slice023Question(question) : baseQuestion(question); }
export function validateBatchABrowserQuestions(questions = []) { if (!questions.some(isCurrent)) return baseQuestions(questions); const errors = []; questions.forEach((question, index) => errors.push(...validateBatchABrowserQuestion(question).errors.map((error) => ({ ...error, path: `questions[${index}].${error.path}` })))); return { ok: errors.length === 0, errors, warnings: [], infos: [], validatorVersion: "p03f23-g6a-u02-reciprocal-v1", validatedAt: null }; }
