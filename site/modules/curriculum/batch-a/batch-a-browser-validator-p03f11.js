export * from "./batch-a-browser-validator-p03f10.js";
import { validateBatchABrowserPlan as validateBasePlan, validateBatchABrowserQuestion as validateBaseQuestion, validateBatchABrowserQuestions as validateBaseQuestions } from "./batch-a-browser-validator-p03f10.js";
import { getBatchABrowserPatternDefinition } from "./source-pattern-full-product-p03f11-extension.js";
import { validateG4BU06DecimalMultiplicationQuestion } from "./one-decimal-times-integer-runtime.js";
import { G4B_U06_SOURCE_ID, G4B_U06_NUMERIC_SPEC_ID, G4B_U06_APPLICATION_SPEC_ID } from "../registry/g4b-u06-one-decimal-times-integer-selector-projection.js";
const ids = [G4B_U06_NUMERIC_SPEC_ID, G4B_U06_APPLICATION_SPEC_ID];
const patternId = (question = {}) => question.patternSpecId ?? question.metadata?.patternId;
const isP03F11 = (question = {}) => ids.includes(patternId(question));
const issue = (code, path) => ({ code, severity: "error", path, message: code });
export function validateBatchABrowserPlan(plan = {}) { const planIds = Array.isArray(plan.patternSpecIds) ? plan.patternSpecIds : []; if (plan.sourceId !== G4B_U06_SOURCE_ID || planIds.length !== 1 || !ids.includes(planIds[0])) return validateBasePlan(plan); const errors = []; if (!Number.isInteger(plan.questionCount) || plan.questionCount <= 0 || plan.questionCount > 8) errors.push(issue("p03f11_question_count_invalid", "questionCount")); const expectedMode = planIds[0] === G4B_U06_APPLICATION_SPEC_ID ? "application" : "numeric"; if (plan.questionMode !== expectedMode) errors.push(issue("p03f11_question_mode_mismatch", "questionMode")); if (plan.genericFallbackAllowed !== false) errors.push(issue("p03f11_generic_fallback_must_be_disabled", "genericFallbackAllowed")); const definition = getBatchABrowserPatternDefinition(planIds[0]); if (!definition || definition.requestedUnknownRole !== "product" || JSON.stringify(definition.givenRoles) !== JSON.stringify(["decimalFactor", "integerFactor"])) errors.push(issue("p03f11_pattern_definition_invalid", "patternSpecIds")); return { ok: errors.length === 0, errors, warnings: [] }; }
export function validateBatchABrowserQuestion(question = {}) { return isP03F11(question) ? validateG4BU06DecimalMultiplicationQuestion(question) : validateBaseQuestion(question); }
export function validateBatchABrowserQuestions(questions = []) { if (!questions.some(isP03F11)) return validateBaseQuestions(questions); const errors = [], warnings = []; for (const [index, question] of questions.entries()) { const result = validateBatchABrowserQuestion(question); errors.push(...result.errors.map((entry) => ({ ...entry, path: `questions[${index}].${entry.path}` }))); warnings.push(...result.warnings); } return { ok: errors.length === 0, errors, warnings, infos: [], validatorVersion: "p03f11-g4b-u06-decimal-multiplication-v1", validatedAt: null }; }
