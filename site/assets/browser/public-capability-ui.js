import "./global-public-layout-controls.js";
import { PUBLIC_UI_SAFE_QUESTION_COUNT, PUBLIC_UI_SURFACES, resolvePublicUiCapabilityBinding } from "../../modules/curriculum/public/public-ui-capability-binding-p04f29.js";
const sourceSelect=document.getElementById("batch-a-source-select"),selectionModeSelect=document.getElementById("batch-a-selection-mode-select"),knowledgePointPanel=document.getElementById("batch-a-knowledge-point-panel"),patternGroupPanel=document.getElementById("batch-a-pattern-group-panel"),section=document.getElementById("g5a-u08-public-controls"),questionSelect=document.getElementById("g5a-u08-question-mode"),depthSelect=document.getElementById("g5a-u08-depth-mode"),contextSelect=document.getElementById("g5a-u08-context-mode"),questionCountInput=document.getElementById("batch-a-question-count-input"),questionField=questionSelect?.closest("label"),depthField=depthSelect?.closest("label"),contextField=contextSelect?.closest("label"),heading=section?.querySelector("h3"),help=section?.querySelector(".help-text"),surfaceId=document.body?.dataset.routeKind==="404"?PUBLIC_UI_SURFACES.FALLBACK_404:PUBLIC_UI_SURFACES.CLASSIC;let applying=false,scheduled=false,lastBindingSignature="",currentBinding=null;
function selectedDataIds(panel,attribute){if(!panel)return[];const property=attribute.replace(/-([a-z])/g,(_,letter)=>letter.toUpperCase());return[...panel.querySelectorAll(`[data-${attribute}][data-selected="true"]`)].map((element)=>element.dataset[property]).filter(Boolean);}function populate(select,options,fallbackValue=null){if(!select)return null;const current=select.value;select.replaceChildren();for(const row of options){const option=document.createElement("option");option.value=row.value;option.textContent=row.label;select.append(option);}const nextValue=options.some((row)=>row.value===current)?current:options.some((row)=>row.value===fallbackValue)?fallbackValue:options[0]?.value??"";select.value=nextValue;select.disabled=options.length<=1;return nextValue;}function setFieldVisibility(field,visible){if(!field)return;field.hidden=!visible;field.dataset.visible=visible?"true":"false";}function unitLabel(){return sourceSelect?.selectedOptions?.[0]?.textContent?.trim()??"目前單元";}function clampQuestionCount(binding=currentBinding){if(!questionCountInput)return;const contract=binding?.questionCount??PUBLIC_UI_SAFE_QUESTION_COUNT,min=Math.max(1,Number(contract.min??1)),max=Math.max(min,Number(contract.max??PUBLIC_UI_SAFE_QUESTION_COUNT.max)),fallback=Math.min(max,Math.max(min,Number(contract.default??max))),current=Number(questionCountInput.value);questionCountInput.min=String(min);questionCountInput.max=String(max);questionCountInput.value=String(Number.isFinite(current)?Math.min(max,Math.max(min,Math.trunc(current))):fallback);questionCountInput.dataset.capacityStatus=String(binding?.capacityStatus??"FAIL_CLOSED_PENDING_PGC_R03").toLowerCase();questionCountInput.dataset.capacityRouteIds=(binding?.capacityRouteIds??[]).join(",");}function applyPatternGroupCompatibility(binding){if(!patternGroupPanel)return;const compatible=new Set(binding.compatiblePatternGroupIds);for(const button of patternGroupPanel.querySelectorAll("[data-pattern-group-id]")){const isCompatible=compatible.has(button.dataset.patternGroupId);button.hidden=!isCompatible;button.disabled=!isCompatible;button.dataset.compatible=isCompatible?"true":"false";button.setAttribute("aria-hidden",isCompatible?"false":"true");if(!isCompatible&&button.dataset.selected==="true")button.dataset.selected="false";}patternGroupPanel.dataset.compatiblePatternGroupCount=String(compatible.size);}function bindingInput(){return{sourceId:sourceSelect?.value??null,surfaceId,selectionMode:selectionModeSelect?.value??"sourceUnit",selectedKnowledgePointIds:selectedDataIds(knowledgePointPanel,"knowledge-point-id"),selectedPatternGroupIds:selectedDataIds(patternGroupPanel,"pattern-group-id"),requestedQuestionType:questionSelect?.value??null,requestedDepthMode:depthSelect?.value??null,requestedContextMode:contextSelect?.value??null};}function bindingSignature(binding){return JSON.stringify({sourceId:binding.sourceId,surfaceId:binding.surfaceId,selectionMode:binding.selectionMode,selectedKnowledgePointIds:binding.selectedKnowledgePointIds,questionType:binding.questionType,questionTypes:binding.availableQuestionTypeOptions.map((row)=>row.value),patternGroupIds:binding.compatiblePatternGroupIds,depth:binding.depthOptions.map((row)=>row.value),context:binding.contextOptions.map((row)=>row.value),depthMode:binding.depthMode,contextMode:binding.contextMode,max:binding.questionCount.max,capacityStatus:binding.capacityStatus,blocked:binding.blockedReasons});}
export function syncPublicCapabilityUi(){if(applying)return null;applying=true;try{const initial=resolvePublicUiCapabilityBinding(bindingInput()),selectedQuestionType=populate(questionSelect,initial.availableQuestionTypeOptions,initial.questionType),typed=selectedQuestionType===initial.questionType?initial:resolvePublicUiCapabilityBinding({...bindingInput(),requestedQuestionType:selectedQuestionType}),selectedDepth=populate(depthSelect,typed.depthOptions,typed.depthMode),selectedContext=populate(contextSelect,typed.contextOptions,typed.contextMode),binding=resolvePublicUiCapabilityBinding({...bindingInput(),requestedQuestionType:selectedQuestionType,requestedDepthMode:selectedDepth,requestedContextMode:selectedContext});currentBinding=binding;clampQuestionCount(binding);const visible=!binding.blocked&&binding.availableQuestionTypeOptions.length>0;if(section){section.dataset.visible=visible?"true":"false";section.dataset.sourceId=binding.sourceId??"";section.dataset.surfaceId=binding.surfaceId;section.dataset.questionType=binding.questionType??"";section.dataset.blocked=binding.blocked?"true":"false";section.dataset.capacityMax=String(binding.questionCount.max);section.dataset.capacityStatus=binding.capacityStatus;section.setAttribute("aria-label",`${unitLabel()}題目能力設定`);}setFieldVisibility(questionField,binding.availableQuestionTypeOptions.length>0);setFieldVisibility(depthField,binding.depthOptions.length>0);setFieldVisibility(contextField,binding.contextOptions.length>0);applyPatternGroupCompatibility(binding);if(heading)heading.textContent=`${unitLabel()}｜可用題目能力`;if(help)help.textContent=binding.blocked?`目前組合不可產生：${binding.blockedReasons.join("、")}`:`已套用公開題數上限；目前上限 ${binding.questionCount.max} 題。`;const signature=bindingSignature(binding);if(signature!==lastBindingSignature){lastBindingSignature=signature;document.dispatchEvent(new CustomEvent("public:capability-binding",{detail:{...binding,availableQuestionTypeOptions:binding.availableQuestionTypeOptions.map((row)=>({...row})),compatiblePatternGroups:binding.compatiblePatternGroups.map((row)=>({...row})),depthOptions:binding.depthOptions.map((row)=>({...row})),contextOptions:binding.contextOptions.map((row)=>({...row}))}}));}return binding;}finally{applying=false;}}function scheduleSync(){if(scheduled)return;scheduled=true;queueMicrotask(()=>{scheduled=false;syncPublicCapabilityUi();});}for(const element of[sourceSelect,selectionModeSelect,questionSelect,depthSelect,contextSelect,questionCountInput])element?.addEventListener("change",scheduleSync);questionCountInput?.addEventListener("input",()=>clampQuestionCount(currentBinding));for(const panel of[knowledgePointPanel,patternGroupPanel,sourceSelect]){if(!panel)continue;new MutationObserver(scheduleSync).observe(panel,{childList:true,subtree:true,attributes:true,attributeFilter:["data-selected","data-source-id"]});}scheduleSync();
